"""bartorch.tools._generated — Auto-generated BART CLI wrappers.

Generated by ``build_tools/gen_tools.py``.  Do not edit by hand.

Each function wraps one BART command.  All functions:

* Accept named keyword arguments matching each BART flag (with type hints).
* Are decorated with ``@bart_op`` for automatic ``complex64`` normalisation.
* Accept ``**extra_flags`` for flags not listed as named parameters.
* Return a plain ``complex64 torch.Tensor`` (or tuple) in C-order.
* Carry a CUDA compatibility note in their docstring.

Source-based generation: yes (bart/src/ parsed)

.. rubric:: Fourier Transforms

.. autosummary::
   :nosignatures:

   fft
   fftmod
   fftshift

.. rubric:: Simulation / Phantoms

.. autosummary::
   :nosignatures:

   phantom
   signal
   bloch

.. rubric:: Sampling / Trajectories

.. autosummary::
   :nosignatures:

   traj
   poisson
   pattern
   upat
   estdelay

.. rubric:: Non-Cartesian

.. autosummary::
   :nosignatures:

   nufft
   nufftbase

.. rubric:: Coil Processing

.. autosummary::
   :nosignatures:

   coils
   cc
   ccapply
   walsh
   whiten
   rss

.. rubric:: Calibration

.. autosummary::
   :nosignatures:

   ecalib
   caldir
   calmat
   ecaltwo
   estvar
   estdims

.. rubric:: Image Processing

.. autosummary::
   :nosignatures:

   resize
   reshape
   crop
   flip
   circshift
   transpose
   repmat
   join
   extract
   squeeze
   flatten
   copy
   slice
   fmac
   scale
   saxpy

.. rubric:: Wavelets / Sparsity

.. autosummary::
   :nosignatures:

   wavelet
   cdf97
   threshold
   tgv
   rof

.. rubric:: Low-Rank / Subspace

.. autosummary::
   :nosignatures:

   svd
   lrmatrix
   casorati
   ssa

.. rubric:: Iterative Algorithms

.. autosummary::
   :nosignatures:

   pics
   pocsense
   itsense
   sake
   nlinv
   moba
   mobafit

.. rubric:: Utilities

.. autosummary::
   :nosignatures:

   bitmask
   avg
   noise
   nrmse
   normalize
   std
   sdot
   zeros
   delta
   ones
   cabs
   carg
   creal
   conj
   cpyphs
   spow
   zexp
   poly
   calc
   invert
   window
   filter
   homodyne
   rmfreq
   mandelbrot

.. rubric:: Miscellaneous

.. autosummary::
   :nosignatures:

   affinereg
   bin
   compress
   conv
   conway
   cunet
   denoise
   epg
   estmotion
   estscaling
   estshift
   fakeksp
   fftrot
   fovshift
   gmm
   grid
   grog
   hist
   ictv
   index
   interpolate
   ismrmrd
   looklocker
   measure
   mip
   mnist
   mobasig
   morphop
   multicfl
   ncalib
   nlinvnet
   nlmeans
   nnet
   onehotenc
   phasepole
   pol2mask
   psf
   pulse
   raga
   reconet
   roistat
   rovir
   rtnlinv
   sample
   seq
   show
   sim
   sqpics
   stl
   tee
   tensorflow
   toimg
   toraw
   trajcor
   twixread
   unwrap
   var
   vec
   wave
   wavepsf
   wshfl

"""

from __future__ import annotations

from typing import Any

import torch

from bartorch.core.graph import dispatch
from bartorch.core.tensor import bart_op


@bart_op
def affinereg(
    reference: torch.Tensor,
    moved: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    mask_reference: str | None = None,
    mask_moved: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Affine registration of reference of <input> and  <moved>.

    Equivalent to calling ``bart affinereg`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    reference : torch.Tensor
        Input CFL array ``reference``.
    moved : torch.Tensor
        Input CFL array ``moved``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: use gpu (if available) Default ``False``.
    mask_reference : str | None
        BART flag ``--mask-reference file``: binary mask for the reference
        image Default ``None``.
    mask_moved : str | None
        BART flag ``--mask-moved file``: binary mask for the moved image
        Default ``None``.
    **extra_flags : Any
        Additional BART ``affinereg`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "affinereg",
        [reference, moved],
        output_dims,
        g=g or None,
        mask_reference=mask_reference,
        mask_moved=mask_moved,
        **extra_flags,
    )


@bart_op
def avg(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    w: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Calculates (weighted) average along dimensions specified by bitmask.

    Equivalent to calling ``bart avg`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    w : bool
        BART flag ``-w``: weighted average Default ``False``.
    **extra_flags : Any
        Additional BART ``avg`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("avg", [input_], output_dims, w=w or None, bitmask=bitmask, **extra_flags)


@bart_op
def bin(
    label: torch.Tensor,
    src: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    o: bool = False,
    M: bool = False,
    l: int | None = None,
    R: int | None = None,
    C: int | None = None,
    r: tuple[int, int] | None = None,
    c: tuple[int, int] | None = None,
    a: int | None = None,
    A: int | None = None,
    O: tuple[float, float] | None = None,
    x: str | None = None,
    stream: bool = False,
    zero_fill: tuple[int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Binning

    Equivalent to calling ``bart bin`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    label : torch.Tensor
        Input CFL array ``label``.
    src : torch.Tensor
        Input CFL array ``src``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    o : bool
        BART flag ``-o``: Reorder according to labels Default ``False``.
    M : bool
        BART flag ``-M``: Amplitude binning Default ``False``.
    l : int | None
        BART flag ``-l dim``: Bin according to labels: Specify cluster
        dimension Default ``None``.
    R : int | None
        BART flag ``-R n_resp``: Quadrature Binning: Number of respiratory
        labels Default ``None``.
    C : int | None
        BART flag ``-C n_card``: Quadrature Binning: Number of cardiac labels
        Default ``None``.
    r : tuple[int, int] | None
        BART flag ``-r x:y``: (Respiration: Eigenvector index) Default
        ``None``.
    c : tuple[int, int] | None
        BART flag ``-c x:y``: (Cardiac motion: Eigenvector index) Default
        ``None``.
    a : int | None
        BART flag ``-a window``: Quadrature Binning: Moving average Default
        ``None``.
    A : int | None
        BART flag ``-A window``: (Quadrature Binning: Cardiac moving average
        window) Default ``None``.
    O : tuple[float, float] | None
        BART flag ``-O [r:c]deg``: Quadrature Binning: Angle offset for resp
        and card. Default ``None``.
    x : str | None
        BART flag ``-x file``: (Output filtered cardiac EOFs) Default ``None``.
    stream : bool
        BART flag ``--stream``: Stream input/output. Default ``False``.
    zero_fill : tuple[int, int] | None
        BART flag ``--zero-fill <dim>:<num_of_frames>``: Specify dimension and
        number of zero filled frames. Zero-filling according to order in
        label_file. Default ``None``.
    **extra_flags : Any
        Additional BART ``bin`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "bin",
        [label, src],
        output_dims,
        o=o or None,
        M=M or None,
        l=l,
        R=R,
        C=C,
        r=r,
        c=c,
        a=a,
        A=A,
        O=O,
        x=x,
        stream=stream or None,
        zero_fill=zero_fill,
        **extra_flags,
    )


@bart_op
def bitmask(
    *,
    output_dims: list[int] | None = None,
    b: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Convert between a bitmask and set of dimensions.

    Equivalent to calling ``bart bitmask`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    b : bool
        BART flag ``-b``: dimensions from bitmask, use with exactly one
        argument Default ``False``.
    **extra_flags : Any
        Additional BART ``bitmask`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("bitmask", [], output_dims, b=b or None, **extra_flags)


@bart_op
def bloch(
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    simulation tool

    Equivalent to calling ``bart bloch`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``bloch`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("bloch", [], output_dims, **extra_flags)


@bart_op
def cabs(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Absolute value of array (|<input>|).

    Equivalent to calling ``bart cabs`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``cabs`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("cabs", [input_], output_dims, **extra_flags)


@bart_op
def calc(
    input_: torch.Tensor,
    func: str,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform function evaluation on array.

    Equivalent to calling ``bart calc`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    func : str
        Positional argument ``func``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``calc`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("calc", [input_], output_dims, func=func, **extra_flags)


@bart_op
def caldir(
    input_: torch.Tensor,
    cal_size: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimates coil sensitivities from the k-space center using

    Equivalent to calling ``bart caldir`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    cal_size : int
        Positional argument ``cal_size``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``caldir`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("caldir", [input_], output_dims, cal_size=cal_size, **extra_flags)


@bart_op
def calmat(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    C: bool = False,
    k: tuple[int, int, int] | None = None,
    K: tuple[int, int, int] | None = None,
    r: tuple[int, int, int] | None = None,
    R: tuple[int, int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute calibration matrix.

    Equivalent to calling ``bart calmat`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    C : bool
        BART flag ``-C``: () Default ``False``.
    k : tuple[int, int, int] | None
        BART flag ``-k ksize``: kernel size Default ``None``.
    K : tuple[int, int, int] | None
        BART flag ``-K``: () Default ``None``.
    r : tuple[int, int, int] | None
        BART flag ``-r cal_size``: Limits the size of the calibration region.
        Default ``None``.
    R : tuple[int, int, int] | None
        BART flag ``-R``: () Default ``None``.
    **extra_flags : Any
        Additional BART ``calmat`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("calmat", [kspace], output_dims, C=C or None, k=k, K=K, r=r, R=R, **extra_flags)


@bart_op
def carg(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Argument (phase angle).

    Equivalent to calling ``bart carg`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``carg`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("carg", [input_], output_dims, **extra_flags)


@bart_op
def casorati(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Casorati matrix with kernel (kern1, ..., kernN) along dimensions (dim1, ...,
    dimN).

    Equivalent to calling ``bart casorati`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``casorati`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("casorati", [input_], output_dims, **extra_flags)


@bart_op
def cc(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    M: bool = False,
    A: bool = False,
    p: int | None = None,
    r: tuple[int, int, int] | None = None,
    R: tuple[int, int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Performs coil compression.

    Equivalent to calling ``bart cc`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    M : bool
        BART flag ``-M``: output compression matrix Default ``False``.
    A : bool
        BART flag ``-A``: use all data to compute coefficients Default
        ``False``.
    p : int | None
        BART flag ``-p N``: perform compression to N virtual channels Default
        ``None``.
    r : tuple[int, int, int] | None
        BART flag ``-r S``: size of calibration region Default ``None``.
    R : tuple[int, int, int] | None
        BART flag ``-R``: (size of calibration region) Default ``None``.
    **extra_flags : Any
        Additional BART ``cc`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "cc", [kspace], output_dims, M=M or None, A=A or None, p=p, r=r, R=R, **extra_flags
    )


@bart_op
def ccapply(
    kspace: torch.Tensor,
    cc_matrix: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    u: bool = False,
    t: bool = False,
    p: int | None = None,
    A: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Apply coil compression forward/inverse operation.

    Equivalent to calling ``bart ccapply`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    cc_matrix : torch.Tensor
        Input CFL array ``cc_matrix``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    u : bool
        BART flag ``-u``: apply inverse operation Default ``False``.
    t : bool
        BART flag ``-t``: don't apply FFT in readout Default ``False``.
    p : int | None
        BART flag ``-p N``: perform compression to N virtual channels Default
        ``None``.
    A : int | None
        BART flag ``-A dim``: Perform alignment of coil sensitivities along
        dimension A Default ``None``.
    **extra_flags : Any
        Additional BART ``ccapply`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "ccapply",
        [kspace, cc_matrix],
        output_dims,
        u=u or None,
        t=t or None,
        p=p,
        A=A,
        **extra_flags,
    )


@bart_op
def cdf97(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    i: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform a wavelet (cdf97) transform.

    Equivalent to calling ``bart cdf97`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    i : bool
        BART flag ``-i``: inverse Default ``False``.
    **extra_flags : Any
        Additional BART ``cdf97`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("cdf97", [input_], output_dims, i=i or None, bitmask=bitmask, **extra_flags)


@bart_op
def circshift(
    input_: torch.Tensor,
    dim: int,
    shift: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform circular shift along {dim} by {shift} elements.

    Equivalent to calling ``bart circshift`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dim : int
        Positional argument ``dim``.
    shift : int
        Positional argument ``shift``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``circshift`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("circshift", [input_], output_dims, dim=dim, shift=shift, **extra_flags)


@bart_op
def coils(
    *,
    output_dims: list[int] | None = None,
    k: bool = False,
    L: bool = False,
    n: int | None = None,
    b: int | None = None,
    t: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute coil sensitivitity maps in x-space or k-space.

    Equivalent to calling ``bart coils`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    k : bool
        BART flag ``-k``: Compute for k-space. Default ``False``.
    L : bool
        BART flag ``-L``: (Legacy: Coil Coefficients for FOV [-1,1].) Default
        ``False``.
    n : int | None
        BART flag ``-n``: Select first n coil channels. Default ``None``.
    b : int | None
        BART flag ``-b``: Bitmask for selecting subset of channels. Default
        ``None``.
    t : str | None
        BART flag ``-t grid``: grid on which sensitivity maps should be
        evaluated. Default ``None``.
    **extra_flags : Any
        Additional BART ``coils`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "coils", [], output_dims, k=k or None, L=L or None, n=n, b=b, t=t, **extra_flags
    )


@bart_op
def compress(
    input_: torch.Tensor,
    mask: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    d: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compress data using a binary mask (pattern)

    Equivalent to calling ``bart compress`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    mask : torch.Tensor
        Input CFL array ``mask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    d : bool
        BART flag ``-d``: decompress data Default ``False``.
    **extra_flags : Any
        Additional BART ``compress`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("compress", [input_, mask], output_dims, d=d or None, **extra_flags)


@bart_op
def conj(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute complex conjugate.

    Equivalent to calling ``bart conj`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``conj`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("conj", [input_], output_dims, **extra_flags)


@bart_op
def conv(
    input_: torch.Tensor,
    kernel: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Performs a convolution along selected dimensions.

    Equivalent to calling ``bart conv`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    kernel : torch.Tensor
        Input CFL array ``kernel``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``conv`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("conv", [input_, kernel], output_dims, bitmask=bitmask, **extra_flags)


@bart_op
def conway(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    P: bool = False,
    n: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Conway's game of life.

    Equivalent to calling ``bart conway`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    P : bool
        BART flag ``-P``: periodic boundary conditions Default ``False``.
    n : int | None
        BART flag ``-n #``: nr. of iterations Default ``None``.
    **extra_flags : Any
        Additional BART ``conway`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("conway", [input_], output_dims, P=P or None, n=n, **extra_flags)


@bart_op
def copy(
    input_: torch.Tensor,
    output: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Copy an array (to a given position in the output file - which then must
    exist).

    Equivalent to calling ``bart copy`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output : torch.Tensor
        Input CFL array ``output``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``copy`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("copy", [input_, output], output_dims, **extra_flags)


@bart_op
def cpyphs(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Copy phase from <input> to <output>.

    Equivalent to calling ``bart cpyphs`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``cpyphs`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("cpyphs", [input_], output_dims, **extra_flags)


@bart_op
def creal(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Real value.

    Equivalent to calling ``bart creal`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``creal`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("creal", [input_], output_dims, **extra_flags)


@bart_op
def crop(
    input_: torch.Tensor,
    dimension: int,
    size: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Extracts a sub-array corresponding to the central part of {size} along
    {dimension}

    Equivalent to calling ``bart crop`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dimension : int
        Positional argument ``dimension``.
    size : int
        Positional argument ``size``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``crop`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("crop", [input_], output_dims, dimension=dimension, size=size, **extra_flags)


@bart_op
def cunet(
    images: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    n: bool = False,
    r: bool = False,
    l: int | None = None,
    b: int | None = None,
    g: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Train a score network with Conditional-UNet architecture.

    Equivalent to calling ``bart cunet`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    images : torch.Tensor
        Input CFL array ``images``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    n : bool
        BART flag ``-n``: (don't use conditional embedding) Default ``False``.
    r : bool
        BART flag ``-r``: use real-valued network (i.e. with z ~ N(0, I))
        Default ``False``.
    l : int | None
        BART flag ``-l l``: Number of unet levels Default ``None``.
    b : int | None
        BART flag ``-b b``: batch size Default ``None``.
    g : bool
        BART flag ``-g``: run on gpu Default ``False``.
    **extra_flags : Any
        Additional BART ``cunet`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "cunet",
        [images],
        output_dims,
        n=n or None,
        r=r or None,
        l=l,
        b=b,
        g=g or None,
        **extra_flags,
    )


@bart_op
def delta(
    dims: int,
    flags: int,
    size: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Kronecker delta.

    Equivalent to calling ``bart delta`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    dims : int
        Positional argument ``dims``.
    flags : int
        Positional argument ``flags``.
    size : int
        Positional argument ``size``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``delta`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("delta", [], output_dims, dims=dims, flags=flags, size=size, **extra_flags)


@bart_op
def denoise(
    image: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    n: bool = False,
    N: bool = False,
    g: bool = False,
    e: bool = False,
    H: bool = False,
    D: bool = False,
    F: bool = False,
    J: bool = False,
    S: bool = False,
    r: float | None = None,
    s: float | None = None,
    i: int | None = None,
    b: int | None = None,
    d: int | None = None,
    u: float | None = None,
    C: int | None = None,
    w: float | None = None,
    adaptive_stepsize: bool = False,
    asl: bool = False,
    teasl: bool = False,
    fista_last: bool = False,
    theta: tuple[float, float] | None = None,
    alpha: tuple[float, float] | None = None,
    gamma: tuple[float, float] | None = None,
    wavelet: str | None = None,
    fista_pqr: tuple[float, float, float] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform image denoising with regularization.

    Equivalent to calling ``bart denoise`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    image : torch.Tensor
        Input CFL array ``image``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    n : bool
        BART flag ``-n``: disable random wavelet cycle spinning Default
        ``False``.
    N : bool
        BART flag ``-N``: do fully overlapping LLR blocks Default ``False``.
    g : bool
        BART flag ``-g``: use GPU Default ``False``.
    e : bool
        BART flag ``-e``: Scale stepsize based on max. eigenvalue Default
        ``False``.
    H : bool
        BART flag ``-H``: (hogwild) Default ``False``.
    D : bool
        BART flag ``-D``: (ADMM dynamic step size) Default ``False``.
    F : bool
        BART flag ``-F``: (fast) Default ``False``.
    J : bool
        BART flag ``-J``: (ADMM residual balancing) Default ``False``.
    S : bool
        BART flag ``-S``: re-scale the image after denoising Default ``False``.
    r : float | None
        BART flag ``-r lambda``: regularization parameter Default ``None``.
    s : float | None
        BART flag ``-s step``: iteration stepsize Default ``None``.
    i : int | None
        BART flag ``-i iter``: max. number of iterations Default ``None``.
    b : int | None
        BART flag ``-b blk``: Lowrank block size Default ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    u : float | None
        BART flag ``-u rho``: ADMM rho Default ``None``.
    C : int | None
        BART flag ``-C iter``: ADMM max. CG iterations Default ``None``.
    w : float | None
        BART flag ``-w``: inverse scaling of the data Default ``None``.
    adaptive_stepsize : bool
        BART flag ``--adaptive-stepsize``: PRIDU adaptive step size Default
        ``False``.
    asl : bool
        BART flag ``--asl``: ASL reconstruction Default ``False``.
    teasl : bool
        BART flag ``--teasl``: Time-encoded ASL reconstruction Default
        ``False``.
    fista_last : bool
        BART flag ``--fista_last``: end iteration with call to data consistency
        Default ``False``.
    theta : tuple[float, float] | None
        BART flag ``--theta theta1:theta2``: PWI weight for ASL reconstruction
        Default ``None``.
    alpha : tuple[float, float] | None
        BART flag ``--alpha alpha1:alpha0``: regularization parameter for TGV
        and ICTGV reconstruction Default ``None``.
    gamma : tuple[float, float] | None
        BART flag ``--gamma gamma1:gamma2``: regularization parameter for ICTV
        and ICTGV reconstruction Default ``None``.
    wavelet : str | None
        BART flag ``--wavelet name``: wavelet type (haar,dau2,cdf44) Default
        ``None``.
    fista_pqr : tuple[float, float, float] | None
        BART flag ``--fista_pqr p:q:r``: parameters for FISTA acceleration
        Default ``None``.
    **extra_flags : Any
        Additional BART ``denoise`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "denoise",
        [image],
        output_dims,
        n=n or None,
        N=N or None,
        g=g or None,
        e=e or None,
        H=H or None,
        D=D or None,
        F=F or None,
        J=J or None,
        S=S or None,
        r=r,
        s=s,
        i=i,
        b=b,
        d=d,
        u=u,
        C=C,
        w=w,
        adaptive_stepsize=adaptive_stepsize or None,
        asl=asl or None,
        teasl=teasl or None,
        fista_last=fista_last or None,
        theta=theta,
        alpha=alpha,
        gamma=gamma,
        wavelet=wavelet,
        fista_pqr=fista_pqr,
        **extra_flags,
    )


@bart_op
def ecalib(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    S: bool = False,
    W: bool = False,
    I: bool = False,
    flag_1: bool = False,
    P: bool = False,
    N: bool = False,
    A: bool = False,
    O: bool = False,
    V: bool = False,
    C: bool = False,
    g: bool = False,
    a: bool = False,
    t: float | None = None,
    c: float | None = None,
    k: tuple[int, int, int] | None = None,
    K: tuple[int, int, int] | None = None,
    r: tuple[int, int, int] | None = None,
    R: tuple[int, int, int] | None = None,
    m: int | None = None,
    b: float | None = None,
    p: float | None = None,
    n: int | None = None,
    v: float | None = None,
    e: int | None = None,
    d: int | None = None,
    i: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate coil sensitivities using ESPIRiT calibration.

    Equivalent to calling ``bart ecalib`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    S : bool
        BART flag ``-S``: create maps with smooth transitions (Soft-SENSE).
        Default ``False``.
    W : bool
        BART flag ``-W``: soft-weighting of the singular vectors. Default
        ``False``.
    I : bool
        BART flag ``-I``: intensity correction Default ``False``.
    flag_1 : bool
        BART flag ``-1``: perform only first part of the calibration Default
        ``False``.
    P : bool
        BART flag ``-P``: Do not rotate the phase with respect to the first
        principal component Default ``False``.
    N : bool
        BART flag ``-N``: Use phase normalization Default ``False``.
    A : bool
        BART flag ``-A``: Approximate eigenvalue decomposition using Nyström
        Default ``False``.
    O : bool
        BART flag ``-O``: () Default ``False``.
    V : bool
        BART flag ``-V``: () Default ``False``.
    C : bool
        BART flag ``-C``: () Default ``False``.
    g : bool
        BART flag ``-g``: () Default ``False``.
    a : bool
        BART flag ``-a``: Automatically pick thresholds. Default ``False``.
    t : float | None
        BART flag ``-t threshold``: This determined the size of the null-space.
        Default ``None``.
    c : float | None
        BART flag ``-c crop_value``: Crop the sensitivities if the eigenvalue
        is smaller than {crop_value}. Default ``None``.
    k : tuple[int, int, int] | None
        BART flag ``-k ksize``: kernel size Default ``None``.
    K : tuple[int, int, int] | None
        BART flag ``-K``: () Default ``None``.
    r : tuple[int, int, int] | None
        BART flag ``-r cal_size``: Limits the size of the calibration region.
        Default ``None``.
    R : tuple[int, int, int] | None
        BART flag ``-R``: () Default ``None``.
    m : int | None
        BART flag ``-m maps``: Number of maps to compute. Default ``None``.
    b : float | None
        BART flag ``-b``: () Default ``None``.
    p : float | None
        BART flag ``-p``: () Default ``None``.
    n : int | None
        BART flag ``-n``: () Default ``None``.
    v : float | None
        BART flag ``-v variance``: Variance of noise in data. Default ``None``.
    e : int | None
        BART flag ``-e dim``: (Split second step along selected dimension,
        default: selects 2 for 3D and -1 (deactivated) for 2D) Default
        ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    i : int | None
        BART flag ``-i orthiter``: () Default ``None``.
    **extra_flags : Any
        Additional BART ``ecalib`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "ecalib",
        [kspace],
        output_dims,
        S=S or None,
        W=W or None,
        I=I or None,
        flag_1=flag_1 or None,
        P=P or None,
        N=N or None,
        A=A or None,
        O=O or None,
        V=V or None,
        C=C or None,
        g=g or None,
        a=a or None,
        t=t,
        c=c,
        k=k,
        K=K,
        r=r,
        R=R,
        m=m,
        b=b,
        p=p,
        n=n,
        v=v,
        e=e,
        d=d,
        i=i,
        **extra_flags,
    )


@bart_op
def ecaltwo(
    input_: torch.Tensor,
    x: int,
    y: int,
    z: int,
    *,
    output_dims: list[int] | None = None,
    S: bool = False,
    O: bool = False,
    g: bool = False,
    c: float | None = None,
    m: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Second part of ESPIRiT calibration.

    Equivalent to calling ``bart ecaltwo`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    x : int
        Positional argument ``x``.
    y : int
        Positional argument ``y``.
    z : int
        Positional argument ``z``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    S : bool
        BART flag ``-S``: Create maps with smooth transitions (Soft-SENSE).
        Default ``False``.
    O : bool
        BART flag ``-O``: () Default ``False``.
    g : bool
        BART flag ``-g``: () Default ``False``.
    c : float | None
        BART flag ``-c crop_value``: Crop the sensitivities if the eigenvalue
        is smaller than {crop_value}. Default ``None``.
    m : int | None
        BART flag ``-m maps``: Number of maps to compute. Default ``None``.
    **extra_flags : Any
        Additional BART ``ecaltwo`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "ecaltwo",
        [input_],
        output_dims,
        S=S or None,
        O=O or None,
        g=g or None,
        c=c,
        m=m,
        x=x,
        y=y,
        z=z,
        **extra_flags,
    )


@bart_op
def epg(
    *,
    output_dims: list[int] | None = None,
    flag_1: float | None = None,
    flag_2: float | None = None,
    b: float | None = None,
    o: float | None = None,
    r: float | None = None,
    e: float | None = None,
    f: float | None = None,
    s: int | None = None,
    n: int | None = None,
    u: int | None = None,
    v: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Simulate MR pulse sequence based on Extended Phase Graphs (EPG)

    Equivalent to calling ``bart epg`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    flag_1 : float | None
        BART flag ``-1 T1``: T1 [units of time] Default ``None``.
    flag_2 : float | None
        BART flag ``-2 T2``: T2 [units of time] Default ``None``.
    b : float | None
        BART flag ``-b B1``: relative B1 [unitless] Default ``None``.
    o : float | None
        BART flag ``-o OFF``: off-resonance [units of inverse time] Default
        ``None``.
    r : float | None
        BART flag ``-r TR``: repetition time [units of time] Default ``None``.
    e : float | None
        BART flag ``-e TE``: echo time [units of time] Default ``None``.
    f : float | None
        BART flag ``-f FA``: flip angle [degrees] / refocusing FA for CPMG
        Default ``None``.
    s : int | None
        BART flag ``-s SP``: spoiling (0: ideal, 1: conventional RF, 2: random
        RF) Default ``None``.
    n : int | None
        BART flag ``-n N``: number of pulses Default ``None``.
    u : int | None
        BART flag ``-u U``: unknowns as bitmask (0: T1, 1: T2, 2: B1, 3: off-
        res) Default ``None``.
    v : int | None
        BART flag ``-v V``: verbosity level Default ``None``.
    **extra_flags : Any
        Additional BART ``epg`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "epg",
        [],
        output_dims,
        flag_1=flag_1,
        flag_2=flag_2,
        b=b,
        o=o,
        r=r,
        e=e,
        f=f,
        s=s,
        n=n,
        u=u,
        v=v,
        **extra_flags,
    )


@bart_op
def estdelay(
    trajectory: torch.Tensor,
    data: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    R: bool = False,
    B: bool = False,
    p: int | None = None,
    n: int | None = None,
    r: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate gradient delays from radial data.

    Equivalent to calling ``bart estdelay`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    trajectory : torch.Tensor
        Input CFL array ``trajectory``.
    data : torch.Tensor
        Input CFL array ``data``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    R : bool
        BART flag ``-R``: RING method Default ``False``.
    B : bool
        BART flag ``-B``: [RING] Assume B0 eddy currents Default ``False``.
    p : int | None
        BART flag ``-p p``: [RING] Padding Default ``None``.
    n : int | None
        BART flag ``-n n``: [RING] Number of intersecting spokes Default
        ``None``.
    r : float | None
        BART flag ``-r r``: [RING] Central region size Default ``None``.
    **extra_flags : Any
        Additional BART ``estdelay`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "estdelay",
        [trajectory, data],
        output_dims,
        R=R or None,
        B=B or None,
        p=p,
        n=n,
        r=r,
        **extra_flags,
    )


@bart_op
def estdims(
    traj: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate image dimension from non-Cartesian trajectory.

    Equivalent to calling ``bart estdims`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    traj : torch.Tensor
        Input CFL array ``traj``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``estdims`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("estdims", [traj], output_dims, **extra_flags)


@bart_op
def estmotion(
    reference: torch.Tensor,
    moved: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    l: int | None = None,
    r: float | None = None,
    optical_flow: bool = False,
    max_flow: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Non-rigid registration with greedy SyN or optical flow algorithm.

    Equivalent to calling ``bart estmotion`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    reference : torch.Tensor
        Input CFL array ``reference``.
    moved : torch.Tensor
        Input CFL array ``moved``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: use gpu (if available) Default ``False``.
    l : int | None
        BART flag ``-l``: number of levels in Gaussian pyramide Default
        ``None``.
    r : float | None
        BART flag ``-r lambda``: regularization strength for TV (optical flow)
        Default ``None``.
    optical_flow : bool
        BART flag ``--optical-flow``: use optical flow instead of greedy SyN
        Default ``False``.
    max_flow : float | None
        BART flag ``--max-flow max``: constraint on flow magnitude (optical
        flow) Default ``None``.
    **extra_flags : Any
        Additional BART ``estmotion`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "estmotion",
        [reference, moved],
        output_dims,
        g=g or None,
        l=l,
        r=r,
        optical_flow=optical_flow or None,
        max_flow=max_flow,
        flags=flags,
        **extra_flags,
    )


@bart_op
def estscaling(
    k_space: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    i: bool = False,
    x: tuple[int, int, int] | None = None,
    p: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate scaling from k-space center.

    Equivalent to calling ``bart estscaling`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    k_space : torch.Tensor
        Input CFL array ``k-space``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    i : bool
        BART flag ``-i``: invert scaling (to directly multiply k-space data)
        Default ``False``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: image dimensions Default ``None``.
    p : float | None
        BART flag ``-p p``: use p-percentile for scaling Default ``None``.
    **extra_flags : Any
        Additional BART ``estscaling`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("estscaling", [k_space], output_dims, i=i or None, x=x, p=p, **extra_flags)


@bart_op
def estshift(
    arg1: torch.Tensor,
    arg2: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    f: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate sub-pixel shift.

    Equivalent to calling ``bart estshift`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    arg1 : torch.Tensor
        Input CFL array ``arg1``.
    arg2 : torch.Tensor
        Input CFL array ``arg2``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    f : bool
        BART flag ``-f``: use FoV coordinates Default ``False``.
    **extra_flags : Any
        Additional BART ``estshift`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("estshift", [arg1, arg2], output_dims, f=f or None, flags=flags, **extra_flags)


@bart_op
def estvar(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    k: tuple[int, int, int] | None = None,
    K: tuple[int, int, int] | None = None,
    r: tuple[int, int, int] | None = None,
    R: tuple[int, int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate the noise variance assuming white Gaussian noise.

    Equivalent to calling ``bart estvar`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    k : tuple[int, int, int] | None
        BART flag ``-k ksize``: kernel size Default ``None``.
    K : tuple[int, int, int] | None
        BART flag ``-K``: () Default ``None``.
    r : tuple[int, int, int] | None
        BART flag ``-r cal_size``: Limits the size of the calibration region.
        Default ``None``.
    R : tuple[int, int, int] | None
        BART flag ``-R``: () Default ``None``.
    **extra_flags : Any
        Additional BART ``estvar`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("estvar", [kspace], output_dims, k=k, K=K, r=r, R=R, **extra_flags)


@bart_op
def extract(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Extracts a sub-array along dims from index start to (not including) end.

    Equivalent to calling ``bart extract`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``extract`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("extract", [input_], output_dims, **extra_flags)


@bart_op
def fakeksp(
    image: torch.Tensor,
    kspace: torch.Tensor,
    sens: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    r: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Recreate k-space from image and sensitivities.

    Equivalent to calling ``bart fakeksp`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    image : torch.Tensor
        Input CFL array ``image``.
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    sens : torch.Tensor
        Input CFL array ``sens``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    r : bool
        BART flag ``-r``: replace measured samples with original values Default
        ``False``.
    **extra_flags : Any
        Additional BART ``fakeksp`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("fakeksp", [image, kspace, sens], output_dims, r=r or None, **extra_flags)


@bart_op
def fft(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    u: bool = False,
    i: bool = False,
    n: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Performs a fast Fourier transform (FFT) along selected dimensions.

    Equivalent to calling ``bart fft`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    u : bool
        BART flag ``-u``: unitary Default ``False``.
    i : bool
        BART flag ``-i``: inverse Default ``False``.
    n : bool
        BART flag ``-n``: un-centered Default ``False``.
    **extra_flags : Any
        Additional BART ``fft`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "fft",
        [input_],
        output_dims,
        u=u or None,
        i=i or None,
        n=n or None,
        bitmask=bitmask,
        **extra_flags,
    )


@bart_op
def fftmod(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    b: bool = False,
    i: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Apply 1 -1 modulation along dimensions selected by the {bitmask}.

    Equivalent to calling ``bart fftmod`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    b : bool
        BART flag ``-b``: (deprecated) Default ``False``.
    i : bool
        BART flag ``-i``: inverse Default ``False``.
    **extra_flags : Any
        Additional BART ``fftmod`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "fftmod", [input_], output_dims, b=b or None, i=i or None, bitmask=bitmask, **extra_flags
    )


@bart_op
def fftrot(
    input_: torch.Tensor,
    dim1: int,
    dim2: int,
    theta: float,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Performs a rotation using Fourier transform (FFT) along selected dimensions.

    Equivalent to calling ``bart fftrot`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dim1 : int
        Positional argument ``dim1``.
    dim2 : int
        Positional argument ``dim2``.
    theta : float
        Positional argument ``theta``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``fftrot`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "fftrot", [input_], output_dims, dim1=dim1, dim2=dim2, theta=theta, **extra_flags
    )


@bart_op
def fftshift(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    b: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Apply fftshift along dimensions selected by the {bitmask}.

    Equivalent to calling ``bart fftshift`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    b : bool
        BART flag ``-b``: apply ifftshift Default ``False``.
    **extra_flags : Any
        Additional BART ``fftshift`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("fftshift", [input_], output_dims, b=b or None, bitmask=bitmask, **extra_flags)


@bart_op
def filter(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    G: bool = False,
    Z: bool = False,
    m: int | None = None,
    l: int | None = None,
    a: int | None = None,
    C: int | None = None,
    d: int | None = None,
    b: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Apply filter.

    Equivalent to calling ``bart filter`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    G : bool
        BART flag ``-G``: geometric median Default ``False``.
    Z : bool
        BART flag ``-Z``: set first (forward diff) or last (backward) value to
        zero Default ``False``.
    m : int | None
        BART flag ``-m dim``: median filter along dimension dim Default
        ``None``.
    l : int | None
        BART flag ``-l len``: length of filter Default ``None``.
    a : int | None
        BART flag ``-a dim``: Moving average filter along dimension dim Default
        ``None``.
    C : int | None
        BART flag ``-C dim``: Cumulative sum along dimension dim Default
        ``None``.
    d : int | None
        BART flag ``-d dim``: forward difference along dimension dim Default
        ``None``.
    b : int | None
        BART flag ``-b dim``: backward difference along dimension dim Default
        ``None``.
    **extra_flags : Any
        Additional BART ``filter`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "filter",
        [input_],
        output_dims,
        G=G or None,
        Z=Z or None,
        m=m,
        l=l,
        a=a,
        C=C,
        d=d,
        b=b,
        **extra_flags,
    )


@bart_op
def flatten(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Flatten array to one dimension.

    Equivalent to calling ``bart flatten`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``flatten`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("flatten", [input_], output_dims, **extra_flags)


@bart_op
def flip(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Flip (reverse) dimensions specified by the {bitmask}.

    Equivalent to calling ``bart flip`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``flip`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("flip", [input_], output_dims, bitmask=bitmask, **extra_flags)


@bart_op
def fmac(
    input1: torch.Tensor,
    input2: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    A: bool = False,
    C: bool = False,
    s: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Multiply <input1> and <input2> and accumulate in <output>.

    Equivalent to calling ``bart fmac`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input1 : torch.Tensor
        Input CFL array ``input1``.
    input2 : torch.Tensor
        Input CFL array ``input2``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    A : bool
        BART flag ``-A``: add to existing output (instead of overwriting)
        Default ``False``.
    C : bool
        BART flag ``-C``: conjugate input2 Default ``False``.
    s : int | None
        BART flag ``-s b``: squash dimensions selected by bitmask b Default
        ``None``.
    **extra_flags : Any
        Additional BART ``fmac`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "fmac", [input1, input2], output_dims, A=A or None, C=C or None, s=s, **extra_flags
    )


@bart_op
def fovshift(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    p: bool = False,
    t: str | None = None,
    S: str | None = None,
    s: tuple[float, float, float] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Shifts FOV.

    Equivalent to calling ``bart fovshift`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    p : bool
        BART flag ``-p``: interpret FOV shift in units of pixel instead of
        units of FoV Default ``False``.
    t : str | None
        BART flag ``-t file``: k-space trajectory Default ``None``.
    S : str | None
        BART flag ``-S file``: FOV shift Default ``None``.
    s : tuple[float, float, float] | None
        BART flag ``-s X:Y:Z``: FOV shift Default ``None``.
    **extra_flags : Any
        Additional BART ``fovshift`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("fovshift", [input_], output_dims, p=p or None, t=t, S=S, s=s, **extra_flags)


@bart_op
def gmm(
    weights: torch.Tensor,
    mean_values: torch.Tensor,
    variances: torch.Tensor,
    evaluate_at_these_points: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    _0: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Evaluate Gaussian mixture.

    Equivalent to calling ``bart gmm`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    weights : torch.Tensor
        Input CFL array ``weights``.
    mean_values : torch.Tensor
        Input CFL array ``mean values``.
    variances : torch.Tensor
        Input CFL array ``variances``.
    evaluate_at_these_points : torch.Tensor
        Input CFL array ``evaluate at these points``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    _0 : bool
        BART flag ``--\0``: compute score Default ``False``.
    **extra_flags : Any
        Additional BART ``gmm`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "gmm",
        [weights, mean_values, variances, evaluate_at_these_points],
        output_dims,
        _0=_0 or None,
        **extra_flags,
    )


@bart_op
def grid(
    *,
    output_dims: list[int] | None = None,
    k: bool = False,
    t: str | None = None,
    T: int | None = None,
    b1: tuple[float, float, float] | None = None,
    b2: tuple[float, float, float] | None = None,
    b3: tuple[float, float, float] | None = None,
    bt: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute sampling grid for x-space / k-space (and time).

    Equivalent to calling ``bart grid`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    k : bool
        BART flag ``-k``: Compute k-space grid Default ``False``.
    t : str | None
        BART flag ``-t Trajectory file``: Sampling trajectory for k-space
        Default ``None``.
    T : int | None
        BART flag ``-T T``: Number of time points Default ``None``.
    b1 : tuple[float, float, float] | None
        BART flag ``--b1 f1:f2:f3``: First basis vector Default ``None``.
    b2 : tuple[float, float, float] | None
        BART flag ``--b2 f1:f2:f3``: Second basis vector Default ``None``.
    b3 : tuple[float, float, float] | None
        BART flag ``--b3 f1:f2:f3``: Third basis vector Default ``None``.
    bt : float | None
        BART flag ``--bt f1``: Time step. Default ``None``.
    **extra_flags : Any
        Additional BART ``grid`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "grid", [], output_dims, k=k or None, t=t, T=T, b1=b1, b2=b2, b3=b3, bt=bt, **extra_flags
    )


@bart_op
def grog(
    radial_trajectory: torch.Tensor,
    radial_data: torch.Tensor,
    gridded_trajectory: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    s: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    GROG calibration and gridding of radial data.

    Equivalent to calling ``bart grog`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    radial_trajectory : torch.Tensor
        Input CFL array ``radial trajectory``.
    radial_data : torch.Tensor
        Input CFL array ``radial data``.
    gridded_trajectory : torch.Tensor
        Input CFL array ``gridded trajectory``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    s : int | None
        BART flag ``-s num``: Number of spokes for GROG calibration Default
        ``None``.
    **extra_flags : Any
        Additional BART ``grog`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "grog",
        [radial_trajectory, radial_data, gridded_trajectory],
        output_dims,
        s=s,
        **extra_flags,
    )


@bart_op
def hist(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    c: bool = False,
    s: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute histogram from -0.5 to 0.5.

    Equivalent to calling ``bart hist`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    c : bool
        BART flag ``-c``: complex Default ``False``.
    s : int | None
        BART flag ``-s size``: number of bins Default ``None``.
    **extra_flags : Any
        Additional BART ``hist`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("hist", [input_], output_dims, c=c or None, s=s, bitmask=bitmask, **extra_flags)


@bart_op
def homodyne(
    input_: torch.Tensor,
    dim: int,
    fraction: float,
    *,
    output_dims: list[int] | None = None,
    I: bool = False,
    C: bool = False,
    n: bool = False,
    r: float | None = None,
    P: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform homodyne reconstruction along dimension dim.

    Equivalent to calling ``bart homodyne`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dim : int
        Positional argument ``dim``.
    fraction : float
        Positional argument ``fraction``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    I : bool
        BART flag ``-I``: Input is in image domain Default ``False``.
    C : bool
        BART flag ``-C``: Clear unacquired portion of kspace Default ``False``.
    n : bool
        BART flag ``-n``: use uncentered ffts Default ``False``.
    r : float | None
        BART flag ``-r alpha``: Offset of ramp filter, between 0 and 1. alpha=0
        is a full ramp, alpha=1 is a horizontal line Default ``None``.
    P : str | None
        BART flag ``-P phase_ref>``: Use <phase_ref> as phase reference Default
        ``None``.
    **extra_flags : Any
        Additional BART ``homodyne`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "homodyne",
        [input_],
        output_dims,
        I=I or None,
        C=C or None,
        n=n or None,
        r=r,
        P=P,
        dim=dim,
        fraction=fraction,
        **extra_flags,
    )


@bart_op
def ictv(
    input_: torch.Tensor,
    lambda_: float,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    i: int | None = None,
    u: float | None = None,
    gamma: tuple[float, float] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Infimal convolution of total variation along dims specified by flags.

    Equivalent to calling ``bart ictv`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    lambda_ : float
        Positional argument ``lambda``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    i : int | None
        BART flag ``-i i``: max. iterations Default ``None``.
    u : float | None
        BART flag ``-u rho``: rho in ADMM Default ``None``.
    gamma : tuple[float, float] | None
        BART flag ``--gamma gamma1:gamma2``: gamma1 * || grad (x - z) ||_1,
        gamma2 * || grad z ||_1 Default ``None``.
    **extra_flags : Any
        Additional BART ``ictv`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "ictv",
        [input_],
        output_dims,
        i=i,
        u=u,
        gamma=gamma,
        lambda_=lambda_,
        flags=flags,
        **extra_flags,
    )


@bart_op
def index(
    dim: int,
    size: int,
    *,
    output_dims: list[int] | None = None,
    end: bool = False,
    log: bool = False,
    min_: float | None = None,
    max_: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Create an array counting from 0 to {size-1} in dimensions {dim}.

    Equivalent to calling ``bart index`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    dim : int
        Positional argument ``dim``.
    size : int
        Positional argument ``size``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    end : bool
        BART flag ``--end``: include the endpoint (last value is size) Default
        ``False``.
    log : bool
        BART flag ``--log``: use logarithmic spacing (but specify min and max)
        Default ``False``.
    min_ : float | None
        BART flag ``--min min``: minimum value (default 0) Default ``None``.
    max_ : float | None
        BART flag ``--max max``: maximum value (default size) Default ``None``.
    **extra_flags : Any
        Additional BART ``index`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "index",
        [],
        output_dims,
        end=end or None,
        log=log or None,
        min_=min_,
        max_=max_,
        dim=dim,
        size=size,
        **extra_flags,
    )


@bart_op
def interpolate(
    input_: torch.Tensor,
    coordinates__default___per__displacement_field__per__affine_transform: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    N: bool = False,
    C: bool = False,
    x: tuple[int, int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Interpolate with coordinates, displacement field, or affine transform.

    Equivalent to calling ``bart interpolate`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    coordinates__default___per__displacement_field__per__affine_transform : torch.Tensor
        Input CFL array ``coordinates (default) / displacement field / affine transform``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    N : bool
        BART flag ``-N``: use nearest neighbour interpolation Default
        ``False``.
    C : bool
        BART flag ``-C``: use cubic interpolation Default ``False``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: output dimensions for affine interpolation or
        coordinates Default ``None``.
    **extra_flags : Any
        Additional BART ``interpolate`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "interpolate",
        [input_, coordinates__default___per__displacement_field__per__affine_transform],
        output_dims,
        N=N or None,
        C=C or None,
        x=x,
        flags=flags,
        **extra_flags,
    )


@bart_op
def invert(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    r: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Invert array (1 / <input>). The output is set to zero in case of divide by
    zero.

    Equivalent to calling ``bart invert`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    r : float | None
        BART flag ``-r reg``: regularization Default ``None``.
    **extra_flags : Any
        Additional BART ``invert`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("invert", [input_], output_dims, r=r, **extra_flags)


@bart_op
def ismrmrd(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    _0: bool = False,
    m: int | None = None,
    o: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Import/Export ISMRM raw data files and streams.

    Equivalent to calling ``bart ismrmrd`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    _0 : bool
        BART flag ``--\0``: Use streaming protocols. Default ``False``.
    m : int | None
        BART flag ``-m``: select specific measurement (split by flag) Default
        ``None``.
    o : int | None
        BART flag ``-o idx``: overwrite data idx times Default ``None``.
    **extra_flags : Any
        Additional BART ``ismrmrd`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("ismrmrd", [input_], output_dims, _0=_0 or None, m=m, o=o, **extra_flags)


@bart_op
def itsense(
    sensitivities: torch.Tensor,
    kspace: torch.Tensor,
    pattern: torch.Tensor,
    alpha: float,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    A simplified implementation of iterative sense reconstruction

    Equivalent to calling ``bart itsense`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    sensitivities : torch.Tensor
        Input CFL array ``sensitivities``.
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    pattern : torch.Tensor
        Input CFL array ``pattern``.
    alpha : float
        Positional argument ``alpha``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``itsense`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "itsense", [sensitivities, kspace, pattern], output_dims, alpha=alpha, **extra_flags
    )


@bart_op
def join(
    output: torch.Tensor,
    dimension: int,
    *,
    output_dims: list[int] | None = None,
    a: bool = False,
    s: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Join input files along {dimensions}. All other dimensions must have the same
    size.

    Equivalent to calling ``bart join`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output : torch.Tensor
        Input CFL array ``output``.
    dimension : int
        Positional argument ``dimension``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    a : bool
        BART flag ``-a``: append - only works for cfl files! Default ``False``.
    s : bool
        BART flag ``-s``: join along streamed dimension! Default ``False``.
    **extra_flags : Any
        Additional BART ``join`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "join", [output], output_dims, a=a or None, s=s or None, dimension=dimension, **extra_flags
    )


@bart_op
def looklocker(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    t: float | None = None,
    D: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute T1 map from M_0, M_ss, and R_1*.

    Equivalent to calling ``bart looklocker`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    t : float | None
        BART flag ``-t threshold``: Pixels with M0 values smaller than
        {threshold} are set to zero. Default ``None``.
    D : float | None
        BART flag ``-D delay``: Time between the middle of inversion pulse and
        the first excitation. Default ``None``.
    **extra_flags : Any
        Additional BART ``looklocker`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("looklocker", [input_], output_dims, t=t, D=D, **extra_flags)


@bart_op
def lrmatrix(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    d: bool = False,
    N: bool = False,
    s: bool = False,
    H: bool = False,
    n: bool = False,
    i: int | None = None,
    m: int | None = None,
    f: int | None = None,
    j: int | None = None,
    k: int | None = None,
    l: int | None = None,
    o: str | None = None,
    p: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform (multi-scale) low rank matrix completion

    Equivalent to calling ``bart lrmatrix`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    d : bool
        BART flag ``-d``: perform decomposition instead, ie fully sampled
        Default ``False``.
    N : bool
        BART flag ``-N``: add noise scale to account for Gaussian noise.
        Default ``False``.
    s : bool
        BART flag ``-s``: perform low rank + sparse matrix completion. Default
        ``False``.
    H : bool
        BART flag ``-H``: (hogwild) Default ``False``.
    n : bool
        BART flag ``-n``: (no randshift) Default ``False``.
    i : int | None
        BART flag ``-i iter``: maximum iterations. Default ``None``.
    m : int | None
        BART flag ``-m flags``: which dimensions are reshaped to matrix
        columns. Default ``None``.
    f : int | None
        BART flag ``-f flags``: which dimensions to perform multi-scale
        partition. Default ``None``.
    j : int | None
        BART flag ``-j scale``: block size scaling from one scale to the next
        one. Default ``None``.
    k : int | None
        BART flag ``-k size``: smallest block size Default ``None``.
    l : int | None
        BART flag ``-l size``: perform locally low rank soft thresholding with
        specified block size. Default ``None``.
    o : str | None
        BART flag ``-o out2``: summed over all non-noise scales to create a
        denoised output. Default ``None``.
    p : float | None
        BART flag ``-p``: (rho) Default ``None``.
    **extra_flags : Any
        Additional BART ``lrmatrix`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "lrmatrix",
        [input_],
        output_dims,
        d=d or None,
        N=N or None,
        s=s or None,
        H=H or None,
        n=n or None,
        i=i,
        m=m,
        f=f,
        j=j,
        k=k,
        l=l,
        o=o,
        p=p,
        **extra_flags,
    )


@bart_op
def mandelbrot(
    *,
    output_dims: list[int] | None = None,
    I: bool = False,
    s: int | None = None,
    n: int | None = None,
    t: float | None = None,
    z: float | None = None,
    r: float | None = None,
    i: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute mandelbrot set.

    Equivalent to calling ``bart mandelbrot`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    I : bool
        BART flag ``-I``: Save iterations Default ``False``.
    s : int | None
        BART flag ``-s size``: image size Default ``None``.
    n : int | None
        BART flag ``-n #``: nr. of iterations Default ``None``.
    t : float | None
        BART flag ``-t t``: threshold for divergence Default ``None``.
    z : float | None
        BART flag ``-z z``: zoom Default ``None``.
    r : float | None
        BART flag ``-r r``: offset real Default ``None``.
    i : float | None
        BART flag ``-i i``: offset imag Default ``None``.
    **extra_flags : Any
        Additional BART ``mandelbrot`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "mandelbrot", [], output_dims, I=I or None, s=s, n=n, t=t, z=z, r=r, i=i, **extra_flags
    )


@bart_op
def measure(
    reference: torch.Tensor,
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Measure image quality.

    Equivalent to calling ``bart measure`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    reference : torch.Tensor
        Input CFL array ``reference``.
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``measure`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("measure", [reference, input_], output_dims, **extra_flags)


@bart_op
def mip(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    m: bool = False,
    a: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Maximum (minimum) intensity projection (MIP) along dimensions specified by
    bitmask.

    Equivalent to calling ``bart mip`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    m : bool
        BART flag ``-m``: minimum Default ``False``.
    a : bool
        BART flag ``-a``: do absolute value first Default ``False``.
    **extra_flags : Any
        Additional BART ``mip`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "mip", [input_], output_dims, m=m or None, a=a or None, bitmask=bitmask, **extra_flags
    )


@bart_op
def mnist(
    input_: torch.Tensor,
    weights: torch.Tensor,
    ref_per_output: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    a: bool = False,
    t: bool = False,
    g: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Trains or applies a MNIST network. This network is to demonstrate how a
    neural network can be implemented in BART.

    Equivalent to calling ``bart mnist`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    weights : torch.Tensor
        Input CFL array ``weights``.
    ref_per_output : torch.Tensor
        Input CFL array ``ref/output``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    a : bool
        BART flag ``-a``: apply nnet Default ``False``.
    t : bool
        BART flag ``-t``: trains network Default ``False``.
    g : bool
        BART flag ``-g``: run on gpu Default ``False``.
    **extra_flags : Any
        Additional BART ``mnist`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "mnist",
        [input_, weights, ref_per_output],
        output_dims,
        a=a or None,
        t=t or None,
        g=g or None,
        **extra_flags,
    )


@bart_op
def moba(
    kspace: torch.Tensor,
    TI_per_TE: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    F: bool = False,
    N: bool = False,
    J: bool = False,
    M: bool = False,
    O: bool = False,
    g: bool = False,
    k: bool = False,
    n: bool = False,
    m: int | None = None,
    l: int | None = None,
    i: int | None = None,
    j: float | None = None,
    u: float | None = None,
    C: int | None = None,
    s: float | None = None,
    B: float | None = None,
    b: tuple[float, float] | None = None,
    d: int | None = None,
    f: float | None = None,
    p: str | None = None,
    I: str | None = None,
    t: str | None = None,
    o: float | None = None,
    e: float | None = None,
    no_alpha_min_exp_decay: bool = False,
    normalize_scaling: bool = False,
    R: float | None = None,
    positive_maps: int | None = None,
    not_wav_maps: int | None = None,
    l2_on_parameters: int | None = None,
    pusteps: int | None = None,
    ratio: float | None = None,
    l1val: float | None = None,
    temporal_damping: float | None = None,
    multi_gpu: int | None = None,
    img_dims: tuple[int, int, int] | None = None,
    sobolev_a: float | None = None,
    sobolev_b: float | None = None,
    scale_data: float | None = None,
    scale_psf: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Model-based nonlinear inverse reconstruction

    Equivalent to calling ``bart moba`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    TI_per_TE : torch.Tensor
        Input CFL array ``TI/TE``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    F : bool
        BART flag ``-F``: (T2 mapping using model-based Fast Spin Echo) Default
        ``False``.
    N : bool
        BART flag ``-N``: (normalize) Default ``False``.
    J : bool
        BART flag ``-J``: Stack frames for joint recon Default ``False``.
    M : bool
        BART flag ``-M``: Simultaneous Multi-Slice reconstruction Default
        ``False``.
    O : bool
        BART flag ``-O``: (Output original maps from reconstruction without
        post processing) Default ``False``.
    g : bool
        BART flag ``-g``: use gpu Default ``False``.
    k : bool
        BART flag ``-k``: k-space edge filter for non-Cartesian trajectories
        Default ``False``.
    n : bool
        BART flag ``-n``: (disable normalization of parameter maps for
        thresholding) Default ``False``.
    m : int | None
        BART flag ``-m model``: Select the MGRE model from enum { WF = 0,
        WFR2S, WF2R2S, R2S, PHASEDIFF, ..., WF_fB0, WF_R2S, T1_R2S,
        W_T1_F_T1_RS2 } [default: WFR2S] Default ``None``.
    l : int | None
        BART flag ``-l \b1/-l2``: toggle l1-wavelet or l2 regularization.
        Default ``None``.
    i : int | None
        BART flag ``-i iter``: Number of Newton steps Default ``None``.
    j : float | None
        BART flag ``-j minreg``: Minimum regularization parameter Default
        ``None``.
    u : float | None
        BART flag ``-u rho``: ADMM rho [default: 0.01] Default ``None``.
    C : int | None
        BART flag ``-C iter``: inner iterations Default ``None``.
    s : float | None
        BART flag ``-s step``: step size Default ``None``.
    B : float | None
        BART flag ``-B bound``: lower bound for relaxation Default ``None``.
    b : tuple[float, float] | None
        BART flag ``-b SMO:SC``: B0 field: spatial smooth level; scaling
        [default: 222.; 1.] Default ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    f : float | None
        BART flag ``-f FOV``:  Default ``None``.
    p : str | None
        BART flag ``-p PSF``:  Default ``None``.
    I : str | None
        BART flag ``-I init``: File for initialization Default ``None``.
    t : str | None
        BART flag ``-t traj``: K-space trajectory Default ``None``.
    o : float | None
        BART flag ``-o os``: Oversampling factor for gridding [default: 1.]
        Default ``None``.
    e : float | None
        BART flag ``-e kfilter_strength``: strength for k-space edge filter
        [default: 2e-3] Default ``None``.
    no_alpha_min_exp_decay : bool
        BART flag ``--no_alpha_min_exp_decay``: (Use hard minimum instead of
        exponential decay towards alpha_min) Default ``False``.
    normalize_scaling : bool
        BART flag ``--normalize_scaling``: (normalize scaling by data / PSF)
        Default ``False``.
    R : float | None
        BART flag ``-R redu``: reduction factor Default ``None``.
    positive_maps : int | None
        BART flag ``--positive-maps flag``: Maps with positivity constraint as
        FLAG! Default ``None``.
    not_wav_maps : int | None
        BART flag ``--not-wav-maps d``: Maps removed from wavelet denoising
        (counted from back!) Default ``None``.
    l2_on_parameters : int | None
        BART flag ``--l2-on-parameters flag``: Flag for parameter maps with l2
        norm Default ``None``.
    pusteps : int | None
        BART flag ``--pusteps ud``: Number of partial update steps for IRGNM
        Default ``None``.
    ratio : float | None
        BART flag ``--ratio f:[0;1]``: Ratio of partial updates:
        ratio*<updated-map> + (1-ratio)*<previous-map> Default ``None``.
    l1val : float | None
        BART flag ``--l1val f``: Regularization scaling of l1 wavelet (default:
        1.) Default ``None``.
    temporal_damping : float | None
        BART flag ``--temporal_damping f``: Temporal damping factor. Default
        ``None``.
    multi_gpu : int | None
        BART flag ``--multi-gpu num``: (number of gpus to use) Default
        ``None``.
    img_dims : tuple[int, int, int] | None
        BART flag ``--img_dims x:y:z``: dimensions Default ``None``.
    sobolev_a : float | None
        BART flag ``--sobolev_a``: (a in 1 + a * \\Laplace^-b/2) Default
        ``None``.
    sobolev_b : float | None
        BART flag ``--sobolev_b``: (b in 1 + a * \\Laplace^-b/2) Default
        ``None``.
    scale_data : float | None
        BART flag ``--scale_data``: scaling factor for data Default ``None``.
    scale_psf : float | None
        BART flag ``--scale_psf``: (scaling factor for PSF) Default ``None``.
    **extra_flags : Any
        Additional BART ``moba`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "moba",
        [kspace, TI_per_TE],
        output_dims,
        F=F or None,
        N=N or None,
        J=J or None,
        M=M or None,
        O=O or None,
        g=g or None,
        k=k or None,
        n=n or None,
        m=m,
        l=l,
        i=i,
        j=j,
        u=u,
        C=C,
        s=s,
        B=B,
        b=b,
        d=d,
        f=f,
        p=p,
        I=I,
        t=t,
        o=o,
        e=e,
        no_alpha_min_exp_decay=no_alpha_min_exp_decay or None,
        normalize_scaling=normalize_scaling or None,
        R=R,
        positive_maps=positive_maps,
        not_wav_maps=not_wav_maps,
        l2_on_parameters=l2_on_parameters,
        pusteps=pusteps,
        ratio=ratio,
        l1val=l1val,
        temporal_damping=temporal_damping,
        multi_gpu=multi_gpu,
        img_dims=img_dims,
        sobolev_a=sobolev_a,
        sobolev_b=sobolev_b,
        scale_data=scale_data,
        scale_psf=scale_psf,
        **extra_flags,
    )


@bart_op
def mobafit(
    enc: torch.Tensor,
    echo_per_contrast_images: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    a: bool = False,
    g: bool = False,
    m: int | None = None,
    i: int | None = None,
    M: int | None = None,
    B: str | None = None,
    levenberg_marquardt: bool = False,
    reg_against_init: bool = False,
    liniter: int | None = None,
    min_flag: int | None = None,
    max_flag: int | None = None,
    max_mag_flag: int | None = None,
    b1map: str | None = None,
    b0map: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Pixel-wise fitting of physical signal models.

    Equivalent to calling ``bart mobafit`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    enc : torch.Tensor
        Input CFL array ``enc``.
    echo_per_contrast_images : torch.Tensor
        Input CFL array ``echo/contrast images``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    a : bool
        BART flag ``-a``: fit magnitude of signal model to data Default
        ``False``.
    g : bool
        BART flag ``-g``: use gpu Default ``False``.
    m : int | None
        BART flag ``-m model``: Select the MGRE model from enum { WF = 0,
        WFR2S, WF2R2S, R2S, PHASEDIFF } [default: WFR2S] Default ``None``.
    i : int | None
        BART flag ``-i iter``: Number of IRGNM steps Default ``None``.
    M : int | None
        BART flag ``-M num_lorentzian_pools``: Multi-Pool-Lorentzian - Number
        of pools Default ``None``.
    B : str | None
        BART flag ``-B file``: temporal (or other) basis Default ``None``.
    levenberg_marquardt : bool
        BART flag ``--levenberg-marquardt``: Use Levenberg-Marquardt instead of
        Gauss-Newton Default ``False``.
    reg_against_init : bool
        BART flag ``--reg-against-init``: Use initial guess as regularization
        target with IRGN Default ``False``.
    liniter : int | None
        BART flag ``--liniter iter``: (iterations for solving linearized
        problem) Default ``None``.
    min_flag : int | None
        BART flag ``--min-flag flags``: Apply minimum constraint on selected
        maps Default ``None``.
    max_flag : int | None
        BART flag ``--max-flag flags``: Apply maximum constraint on selected
        maps Default ``None``.
    max_mag_flag : int | None
        BART flag ``--max-mag-flag flags``: Apply maximum magnitude constraint
        on selected maps Default ``None``.
    b1map : str | None
        BART flag ``--b1map [deg]``: Input B1 map as cfl file Default ``None``.
    b0map : str | None
        BART flag ``--b0map [rad/s]``: Input B0 map as cfl file Default
        ``None``.
    **extra_flags : Any
        Additional BART ``mobafit`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "mobafit",
        [enc, echo_per_contrast_images],
        output_dims,
        a=a or None,
        g=g or None,
        m=m,
        i=i,
        M=M,
        B=B,
        levenberg_marquardt=levenberg_marquardt or None,
        reg_against_init=reg_against_init or None,
        liniter=liniter,
        min_flag=min_flag,
        max_flag=max_flag,
        max_mag_flag=max_mag_flag,
        b1map=b1map,
        b0map=b0map,
        **extra_flags,
    )


@bart_op
def mobasig(
    parameters_per_coefficients: torch.Tensor,
    encoding: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    m: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Forward calculation of physical signal models.

    Equivalent to calling ``bart mobasig`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    parameters_per_coefficients : torch.Tensor
        Input CFL array ``parameters/coefficients``.
    encoding : torch.Tensor
        Input CFL array ``encoding``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    m : int | None
        BART flag ``-m model``: Select the MGRE model from enum { WF = 0,
        WFR2S, WF2R2S, R2S, PHASEDIFF } [default: WFR2S] Default ``None``.
    **extra_flags : Any
        Additional BART ``mobasig`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "mobasig", [parameters_per_coefficients, encoding], output_dims, m=m, **extra_flags
    )


@bart_op
def morphop(
    binary_input: torch.Tensor,
    mask_size: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform morphological operators on binary data with odd mask sizes.

    Equivalent to calling ``bart morphop`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    binary_input : torch.Tensor
        Input CFL array ``binary input``.
    mask_size : int
        Positional argument ``mask_size``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``morphop`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("morphop", [binary_input], output_dims, mask_size=mask_size, **extra_flags)


@bart_op
def multicfl(
    *,
    output_dims: list[int] | None = None,
    s: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Combine/Split multiple cfl files to one multi-cfl file.

    Equivalent to calling ``bart multicfl`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    s : bool
        BART flag ``-s``: separate Default ``False``.
    **extra_flags : Any
        Additional BART ``multicfl`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("multicfl", [], output_dims, s=s or None, **extra_flags)


@bart_op
def ncalib(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    o: bool = False,
    N: bool = False,
    t: str | None = None,
    p: str | None = None,
    B: str | None = None,
    r: tuple[int, int, int] | None = None,
    i: int | None = None,
    M: float | None = None,
    a: float | None = None,
    b: float | None = None,
    c: float | None = None,
    w: float | None = None,
    m: int | None = None,
    cgiter: int | None = None,
    cgtol: float | None = None,
    alpha: float | None = None,
    x: tuple[int, int, int] | None = None,
    sens_os: float | None = None,
    shared_img_dims: int | None = None,
    shared_col_dims: int | None = None,
    scale_loop_dims: int | None = None,
    phase_pole: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate coil sensitivities using ENLIVE calibration.

    Equivalent to calling ``bart ncalib`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: use gpu Default ``False``.
    o : bool
        BART flag ``-o``: return oversampled coils Default ``False``.
    N : bool
        BART flag ``-N``: Normalize coil sensitivities Default ``False``.
    t : str | None
        BART flag ``-t file``: kspace trajectory Default ``None``.
    p : str | None
        BART flag ``-p file``: kspace pattern Default ``None``.
    B : str | None
        BART flag ``-B file``: subspace basis Default ``None``.
    r : tuple[int, int, int] | None
        BART flag ``-r cal_size``: Limits the size of the calibration region.
        Default ``None``.
    i : int | None
        BART flag ``-i iter``: Number of Newton steps Default ``None``.
    M : float | None
        BART flag ``-M``: (minimum for regularization) Default ``None``.
    a : float | None
        BART flag ``-a``: (a in 1 + a * \\Laplace^-b/2) Default ``None``.
    b : float | None
        BART flag ``-b``: (b in 1 + a * \\Laplace^-b/2) Default ``None``.
    c : float | None
        BART flag ``-c``: (c in 1 + a * \\Laplace^-b/2) Default ``None``.
    w : float | None
        BART flag ``-w``: (inverse scaling of the data) Default ``None``.
    m : int | None
        BART flag ``-m nmaps``: Number of ENLIVE maps to use in reconstruction
        Default ``None``.
    cgiter : int | None
        BART flag ``--cgiter iter``: (iterations for linearized problem)
        Default ``None``.
    cgtol : float | None
        BART flag ``--cgtol tol``: (tolerance for linearized problem) Default
        ``None``.
    alpha : float | None
        BART flag ``--alpha val``: (alpha in first iteration) Default ``None``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: Explicitly specify sens dimensions Default
        ``None``.
    sens_os : float | None
        BART flag ``--sens-os val``: (over-sampling factor for sensitivities)
        Default ``None``.
    shared_img_dims : int | None
        BART flag ``--shared-img-dims flags``: deselect image dims with flags
        Default ``None``.
    shared_col_dims : int | None
        BART flag ``--shared-col-dims flags``: deselect coil dims with flags
        Default ``None``.
    scale_loop_dims : int | None
        BART flag ``--scale-loop-dims flags``: scale parameters as if ncalib
        was looped over these dims Default ``None``.
    phase_pole : int | None
        BART flag ``--phase-pole d``: Use phase pole detection after d
        iterations (0 for every iteration) Default ``None``.
    **extra_flags : Any
        Additional BART ``ncalib`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "ncalib",
        [kspace],
        output_dims,
        g=g or None,
        o=o or None,
        N=N or None,
        t=t,
        p=p,
        B=B,
        r=r,
        i=i,
        M=M,
        a=a,
        b=b,
        c=c,
        w=w,
        m=m,
        cgiter=cgiter,
        cgtol=cgtol,
        alpha=alpha,
        x=x,
        sens_os=sens_os,
        shared_img_dims=shared_img_dims,
        shared_col_dims=shared_col_dims,
        scale_loop_dims=scale_loop_dims,
        phase_pole=phase_pole,
        **extra_flags,
    )


@bart_op
def nlinv(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    c: bool = False,
    N: bool = False,
    U: bool = False,
    g: bool = False,
    S: bool = False,
    P: bool = False,
    n: bool = False,
    i: int | None = None,
    M: float | None = None,
    d: int | None = None,
    m: int | None = None,
    f: float | None = None,
    p: str | None = None,
    t: str | None = None,
    B: str | None = None,
    I: str | None = None,
    s: int | None = None,
    a: float | None = None,
    b: float | None = None,
    w: float | None = None,
    psf_based: bool = False,
    lowmem: bool = False,
    ret_sens_os: bool = False,
    real_time: bool = False,
    fast: bool = False,
    legacy_early_stopping: bool = False,
    alpha: float | None = None,
    reg_iter: int | None = None,
    x: tuple[int, int, int] | None = None,
    sens_dims: tuple[int, int, int] | None = None,
    ksens_dims: tuple[int, int, int] | None = None,
    sens_os: float | None = None,
    cgiter: int | None = None,
    cgtol: float | None = None,
    liniter: int | None = None,
    phase_pole: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Jointly estimate image and sensitivities with nonlinear

    Equivalent to calling ``bart nlinv`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    c : bool
        BART flag ``-c``: Real-value constraint Default ``False``.
    N : bool
        BART flag ``-N``: Do not normalize image with coil sensitivities
        Default ``False``.
    U : bool
        BART flag ``-U``: Do not combine ENLIVE maps in output Default
        ``False``.
    g : bool
        BART flag ``-g``: use gpu Default ``False``.
    S : bool
        BART flag ``-S``: Re-scale image after reconstruction Default
        ``False``.
    P : bool
        BART flag ``-P``: (supplied psf is different for each coil) Default
        ``False``.
    n : bool
        BART flag ``-n``: (non-Cartesian) Default ``False``.
    i : int | None
        BART flag ``-i iter``: Number of Newton steps Default ``None``.
    M : float | None
        BART flag ``-M``: (minimum for regularization) Default ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    m : int | None
        BART flag ``-m nmaps``: Number of ENLIVE maps to use in reconstruction
        Default ``None``.
    f : float | None
        BART flag ``-f FOV``: restrict FOV Default ``None``.
    p : str | None
        BART flag ``-p file``: pattern / transfer function Default ``None``.
    t : str | None
        BART flag ``-t file``: kspace trajectory Default ``None``.
    B : str | None
        BART flag ``-B file``: temporal (or other) basis Default ``None``.
    I : str | None
        BART flag ``-I file``: File for initialization Default ``None``.
    s : int | None
        BART flag ``-s``: (dimensions with constant sensitivities) Default
        ``None``.
    a : float | None
        BART flag ``-a``: (a in 1 + a * \\Laplace^-b/2) Default ``None``.
    b : float | None
        BART flag ``-b``: (b in 1 + a * \\Laplace^-b/2) Default ``None``.
    w : float | None
        BART flag ``-w``: (inverse scaling of the data) Default ``None``.
    psf_based : bool
        BART flag ``--psf-based``: (use psf based reconstruction) Default
        ``False``.
    lowmem : bool
        BART flag ``--lowmem``: (Use low-mem mode of the nuFFT) Default
        ``False``.
    ret_sens_os : bool
        BART flag ``--ret-sens-os``: (return sensitivities on oversampled grid)
        Default ``False``.
    real_time : bool
        BART flag ``--real-time``: Use real-time (temporal l2) regularization
        Default ``False``.
    fast : bool
        BART flag ``--fast``: Use tuned but less generic model Default
        ``False``.
    legacy_early_stopping : bool
        BART flag ``--legacy-early-stopping``: (legacy mode for irgnm early
        stopping) Default ``False``.
    alpha : float | None
        BART flag ``--alpha val``: (alpha in first iteration) Default ``None``.
    reg_iter : int | None
        BART flag ``--reg-iter iter``: Number of Newton steps with
        regularization (-1 means all) Default ``None``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: Explicitly specify image dimensions Default
        ``None``.
    sens_dims : tuple[int, int, int] | None
        BART flag ``--sens-dims x:y:z``: Explicitly specify sens dimensions
        Default ``None``.
    ksens_dims : tuple[int, int, int] | None
        BART flag ``--ksens-dims x:y:z``: (Explicitly specify kspace-sens
        dimensions) Default ``None``.
    sens_os : float | None
        BART flag ``--sens-os val``: (over-sampling factor for sensitivities)
        Default ``None``.
    cgiter : int | None
        BART flag ``--cgiter iter``: (iterations for linearized problem)
        Default ``None``.
    cgtol : float | None
        BART flag ``--cgtol tol``: (tolerance for linearized problem) Default
        ``None``.
    liniter : int | None
        BART flag ``--liniter iter``: (iterations for solving linearized
        problem) Default ``None``.
    phase_pole : int | None
        BART flag ``--phase-pole d``: Use phase pole detection after d
        iterations (0 for every iteration) Default ``None``.
    **extra_flags : Any
        Additional BART ``nlinv`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "nlinv",
        [kspace],
        output_dims,
        c=c or None,
        N=N or None,
        U=U or None,
        g=g or None,
        S=S or None,
        P=P or None,
        n=n or None,
        i=i,
        M=M,
        d=d,
        m=m,
        f=f,
        p=p,
        t=t,
        B=B,
        I=I,
        s=s,
        a=a,
        b=b,
        w=w,
        psf_based=psf_based or None,
        lowmem=lowmem or None,
        ret_sens_os=ret_sens_os or None,
        real_time=real_time or None,
        fast=fast or None,
        legacy_early_stopping=legacy_early_stopping or None,
        alpha=alpha,
        reg_iter=reg_iter,
        x=x,
        sens_dims=sens_dims,
        ksens_dims=ksens_dims,
        sens_os=sens_os,
        cgiter=cgiter,
        cgtol=cgtol,
        liniter=liniter,
        phase_pole=phase_pole,
        **extra_flags,
    )


@bart_op
def nlinvnet(
    kspace: torch.Tensor,
    weights: torch.Tensor,
    output_per_reference: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    i: int | None = None,
    s: int | None = None,
    L: int | None = None,
    ref_img: bool = False,
    ref_col_rt: bool = False,
    fix_sens: bool = False,
    init_rtnlinv: bool = False,
    t: bool = False,
    a: bool = False,
    rss_norm: bool = False,
    g: bool = False,
    ksp_training: bool = False,
    iter_net: int | None = None,
    sens_os: float | None = None,
    alpha_min: float | None = None,
    lambda_: float | None = None,
    lambda_sens: float | None = None,
    filter_: str | None = None,
    conv_time: int | None = None,
    x: tuple[int, int, int] | None = None,
    pattern: str | None = None,
    trajectory: str | None = None,
    B: str | None = None,
    scaling: float | None = None,
    scaling_flags: int | None = None,
    b: int | None = None,
    l: str | None = None,
    mask: str | None = None,
    train_loss_l2_reg: float | None = None,
    ss_ksp_split: float | None = None,
    ss_ksp_split_shared: int | None = None,
    ss_ksp_use_reco: str | None = None,
    ss_ksp_leaky: float | None = None,
    temporal_train_mask: tuple[int, int] | None = None,
    average_coils_loss: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform NLINV-Net reconstruction.

    Equivalent to calling ``bart nlinvnet`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    weights : torch.Tensor
        Input CFL array ``weights``.
    output_per_reference : torch.Tensor
        Input CFL array ``output/reference``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    i : int | None
        BART flag ``-i iter``: Number of Newton steps Default ``None``.
    s : int | None
        BART flag ``-s``: (dimensions with constant sensitivities) Default
        ``None``.
    L : int | None
        BART flag ``-L flags``: loop over dims (apply only) Default ``None``.
    ref_img : bool
        BART flag ``--ref-img``: (Feed image after initialization in every
        network.) Default ``False``.
    ref_col_rt : bool
        BART flag ``--ref-col-rt``: (Temporal regularization for coil
        sensitivities.) Default ``False``.
    fix_sens : bool
        BART flag ``--fix-sens``: (Fix sensitivity maps after initialization)
        Default ``False``.
    init_rtnlinv : bool
        BART flag ``--init-rtnlinv``: initialize with rtnlinv recon Default
        ``False``.
    t : bool
        BART flag ``-t``: train nlinvnet Default ``False``.
    a : bool
        BART flag ``-a``: apply nlinvnet Default ``False``.
    rss_norm : bool
        BART flag ``--rss-norm``: scale output image to rss normalization
        Default ``False``.
    g : bool
        BART flag ``-g``: run on gpu Default ``False``.
    ksp_training : bool
        BART flag ``--ksp-training``: provide kspace as reference Default
        ``False``.
    iter_net : int | None
        BART flag ``--iter-net iter``: number of iterations with network
        Default ``None``.
    sens_os : float | None
        BART flag ``--sens-os val``: (over-sampling factor for sensitivities)
        Default ``None``.
    alpha_min : float | None
        BART flag ``--alpha-min val``: (minimal value for alpha) Default
        ``None``.
    lambda_ : float | None
        BART flag ``--lambda val``: additional regularization for network part
        (negative means trainable) Default ``None``.
    lambda_sens : float | None
        BART flag ``--lambda-sens val``: additional regularization for
        sensitivities (negative means trainable) Default ``None``.
    filter_ : str | None
        BART flag ``--filter <filter>``: filter output of network block Default
        ``None``.
    conv_time : int | None
        BART flag ``--conv-time w``: convolve along dimension 10 with window
        size w Default ``None``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: image dimensions Default ``None``.
    pattern : str | None
        BART flag ``--pattern <pattern>``: sampling pattern Default ``None``.
    trajectory : str | None
        BART flag ``--trajectory <traj>``: trajectory Default ``None``.
    B : str | None
        BART flag ``-B <basis>``: basis Default ``None``.
    scaling : float | None
        BART flag ``--scaling val``: scaling of data, negative means
        normalization to norm=val Default ``None``.
    scaling_flags : int | None
        BART flag ``--scaling-flags flags``: scaling is increased with
        sqrt(selected dims) Default ``None``.
    b : int | None
        BART flag ``-b``: size of mini batches Default ``None``.
    l : str | None
        BART flag ``-l <weights-init>``: load weights for continuing training
        Default ``None``.
    mask : str | None
        BART flag ``--mask <mask>``: mask for computation of loss Default
        ``None``.
    train_loss_l2_reg : float | None
        BART flag ``--train-loss-l2-reg l``: add l(||x||^2 + ||Wc||^2) to train
        loss Default ``None``.
    ss_ksp_split : float | None
        BART flag ``--ss-ksp-split p``: use p\\% of kspace data as network
        input Default ``None``.
    ss_ksp_split_shared : int | None
        BART flag ``--ss-ksp-split-shared flags``: shared dims for mask Default
        ``None``.
    ss_ksp_use_reco : str | None
        BART flag ``--ss-ksp-use-reco file``: mask should contain 0 or 1.
        Entries with 1 are neverused as loss Default ``None``.
    ss_ksp_leaky : float | None
        BART flag ``--ss-ksp-leaky l``: also use network input scaled by l as
        reference Default ``None``.
    temporal_train_mask : tuple[int, int] | None
        BART flag ``--temporal-train-mask s:e``: Only use data in [s, e) as
        train reference Default ``None``.
    average_coils_loss : int | None
        BART flag ``--average-coils-loss l``: Average coils along window of
        size l for loss computation Default ``None``.
    **extra_flags : Any
        Additional BART ``nlinvnet`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "nlinvnet",
        [kspace, weights, output_per_reference],
        output_dims,
        i=i,
        s=s,
        L=L,
        ref_img=ref_img or None,
        ref_col_rt=ref_col_rt or None,
        fix_sens=fix_sens or None,
        init_rtnlinv=init_rtnlinv or None,
        t=t or None,
        a=a or None,
        rss_norm=rss_norm or None,
        g=g or None,
        ksp_training=ksp_training or None,
        iter_net=iter_net,
        sens_os=sens_os,
        alpha_min=alpha_min,
        lambda_=lambda_,
        lambda_sens=lambda_sens,
        filter_=filter_,
        conv_time=conv_time,
        x=x,
        pattern=pattern,
        trajectory=trajectory,
        B=B,
        scaling=scaling,
        scaling_flags=scaling_flags,
        b=b,
        l=l,
        mask=mask,
        train_loss_l2_reg=train_loss_l2_reg,
        ss_ksp_split=ss_ksp_split,
        ss_ksp_split_shared=ss_ksp_split_shared,
        ss_ksp_use_reco=ss_ksp_use_reco,
        ss_ksp_leaky=ss_ksp_leaky,
        temporal_train_mask=temporal_train_mask,
        average_coils_loss=average_coils_loss,
        **extra_flags,
    )


@bart_op
def nlmeans(
    input_: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    H: float | None = None,
    a: float | None = None,
    p: int | None = None,
    d: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Non-local means filter

    Equivalent to calling ``bart nlmeans`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    H : float | None
        BART flag ``-H h``: NLMeans h Default ``None``.
    a : float | None
        BART flag ``-a a``: NLMeans a (stddev for gaussian euclidean distance)
        Default ``None``.
    p : int | None
        BART flag ``-p int``: patch length Default ``None``.
    d : int | None
        BART flag ``-d int``: patch distance Default ``None``.
    **extra_flags : Any
        Additional BART ``nlmeans`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "nlmeans", [input_], output_dims, H=H, a=a, p=p, d=d, flags=flags, **extra_flags
    )


@bart_op
def nnet(
    input_: torch.Tensor,
    weights: torch.Tensor,
    ref_per_output: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    a: bool = False,
    e: bool = False,
    t: bool = False,
    g: bool = False,
    load_memory: bool = False,
    b: int | None = None,
    l: str | None = None,
    export_graph: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Trains or applies a neural network.

    Equivalent to calling ``bart nnet`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    weights : torch.Tensor
        Input CFL array ``weights``.
    ref_per_output : torch.Tensor
        Input CFL array ``ref/output``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    a : bool
        BART flag ``-a``: apply nnet Default ``False``.
    e : bool
        BART flag ``-e``: evaluate nnet Default ``False``.
    t : bool
        BART flag ``-t``: trains network Default ``False``.
    g : bool
        BART flag ``-g``: run on gpu Default ``False``.
    load_memory : bool
        BART flag ``--load-memory``: load files into memory Default ``False``.
    b : int | None
        BART flag ``-b batchsize``: size of mini batches Default ``None``.
    l : str | None
        BART flag ``-l <weights-init>``: load weights for continuing training
        Default ``None``.
    export_graph : str | None
        BART flag ``--export-graph <file.dot>``: export graph for visualization
        Default ``None``.
    **extra_flags : Any
        Additional BART ``nnet`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "nnet",
        [input_, weights, ref_per_output],
        output_dims,
        a=a or None,
        e=e or None,
        t=t or None,
        g=g or None,
        load_memory=load_memory or None,
        b=b,
        l=l,
        export_graph=export_graph,
        **extra_flags,
    )


@bart_op
def noise(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    r: bool = False,
    s: int | None = None,
    S: float | None = None,
    n: float | None = None,
    uniform: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Add noise with selected variance to input.

    Equivalent to calling ``bart noise`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    r : bool
        BART flag ``-r``: real-valued input Default ``False``.
    s : int | None
        BART flag ``-s``: random seed initialization. '0' uses the default
        seed. Default ``None``.
    S : float | None
        BART flag ``-S``: () Default ``None``.
    n : float | None
        BART flag ``-n variance``: DEFAULT: 1.0 Default ``None``.
    uniform : bool
        BART flag ``--uniform``: select uniform noise distribution Default
        ``False``.
    **extra_flags : Any
        Additional BART ``noise`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "noise",
        [input_],
        output_dims,
        r=r or None,
        s=s,
        S=S,
        n=n,
        uniform=uniform or None,
        **extra_flags,
    )


@bart_op
def normalize(
    input_: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    b: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Normalize along selected dimensions.

    Equivalent to calling ``bart normalize`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    b : bool
        BART flag ``-b``: l1 Default ``False``.
    **extra_flags : Any
        Additional BART ``normalize`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("normalize", [input_], output_dims, b=b or None, flags=flags, **extra_flags)


@bart_op
def nrmse(
    reference: torch.Tensor,
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    s: bool = False,
    t: float | None = None,
    S: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Output normalized root mean square error (NRMSE),

    Equivalent to calling ``bart nrmse`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    reference : torch.Tensor
        Input CFL array ``reference``.
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    s : bool
        BART flag ``-s``: automatic (complex) scaling Default ``False``.
    t : float | None
        BART flag ``-t eps``: compare to eps Default ``None``.
    S : bool
        BART flag ``-S``: use scientific notation in output Default ``False``.
    **extra_flags : Any
        Additional BART ``nrmse`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "nrmse", [reference, input_], output_dims, s=s or None, t=t, S=S or None, **extra_flags
    )


@bart_op
def nufft(
    traj: torch.Tensor,
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    a: bool = False,
    i: bool = False,
    t: bool = False,
    r: bool = False,
    c: bool = False,
    P: bool = False,
    s: bool = False,
    g: bool = False,
    flag_1: bool = False,
    x: tuple[int, int, int] | None = None,
    d: tuple[int, int, int] | None = None,
    D: tuple[int, int, int] | None = None,
    l: float | None = None,
    m: int | None = None,
    B: str | None = None,
    p: str | None = None,
    F: str | None = None,
    T: str | None = None,
    lowmem: bool = False,
    zero_mem: bool = False,
    no_precomp: bool = False,
    o: float | None = None,
    w: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform non-uniform Fast Fourier Transform.

    Equivalent to calling ``bart nufft`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    traj : torch.Tensor
        Input CFL array ``traj``.
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    a : bool
        BART flag ``-a``: adjoint Default ``False``.
    i : bool
        BART flag ``-i``: inverse Default ``False``.
    t : bool
        BART flag ``-t``: Toeplitz embedding for inverse NUFFT Default
        ``False``.
    r : bool
        BART flag ``-r``: turn-off Toeplitz embedding for inverse NUFFT Default
        ``False``.
    c : bool
        BART flag ``-c``: preconditioning for inverse NUFFT Default ``False``.
    P : bool
        BART flag ``-P``: periodic k-space Default ``False``.
    s : bool
        BART flag ``-s``: DFT Default ``False``.
    g : bool
        BART flag ``-g``: GPU Default ``False``.
    flag_1 : bool
        BART flag ``-1``: use/return oversampled grid Default ``False``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: dimensions Default ``None``.
    d : tuple[int, int, int] | None
        BART flag ``-d``: (dimensions, deprecated) Default ``None``.
    D : tuple[int, int, int] | None
        BART flag ``-D``: (dimensions, long deprecated) Default ``None``.
    l : float | None
        BART flag ``-l lambda``: l2 regularization Default ``None``.
    m : int | None
        BART flag ``-m iter``: max. number of iterations (inverse only) Default
        ``None``.
    B : str | None
        BART flag ``-B file``: temporal (or other) basis Default ``None``.
    p : str | None
        BART flag ``-p file``: weighting of nufft Default ``None``.
    F : str | None
        BART flag ``-F file``: b0 inhomogeneity fieldmap Default ``None``.
    T : str | None
        BART flag ``-T file``: timemap for conjugate phase reconstruction
        Default ``None``.
    lowmem : bool
        BART flag ``--lowmem``: use low-mem mode of the nuFFT Default
        ``False``.
    zero_mem : bool
        BART flag ``--zero-mem``: use zero-overhead mode of the nuFFT Default
        ``False``.
    no_precomp : bool
        BART flag ``--no-precomp``: turn off precomputation Default ``False``.
    o : float | None
        BART flag ``-o o``: oversample grid by factor (default: o=2; required
        for Toeplitz) Default ``None``.
    w : float | None
        BART flag ``-w w``: width of Kaiser-Bessel window (default: w=6)
        Default ``None``.
    **extra_flags : Any
        Additional BART ``nufft`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "nufft",
        [traj, input_],
        output_dims,
        a=a or None,
        i=i or None,
        t=t or None,
        r=r or None,
        c=c or None,
        P=P or None,
        s=s or None,
        g=g or None,
        flag_1=flag_1 or None,
        x=x,
        d=d,
        D=D,
        l=l,
        m=m,
        B=B,
        p=p,
        F=F,
        T=T,
        lowmem=lowmem or None,
        zero_mem=zero_mem or None,
        no_precomp=no_precomp or None,
        o=o,
        w=w,
        **extra_flags,
    )


@bart_op
def nufftbase(
    trajectory: torch.Tensor,
    dimensions: tuple[int, int, int],
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute the Fourier transform of a basis function to be used in the nuFFT.

    Equivalent to calling ``bart nufftbase`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    trajectory : torch.Tensor
        Input CFL array ``trajectory``.
    dimensions : tuple[int, int, int]
        Positional argument ``dimensions``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``nufftbase`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("nufftbase", [trajectory], output_dims, dimensions=dimensions, **extra_flags)


@bart_op
def onehotenc(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    r: bool = False,
    i: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Transforms class labels to one-hot-encoded classes

    Equivalent to calling ``bart onehotenc`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    r : bool
        BART flag ``-r``: get class label by maximum entry Default ``False``.
    i : int | None
        BART flag ``-i index``: select dimension Default ``None``.
    **extra_flags : Any
        Additional BART ``onehotenc`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("onehotenc", [input_], output_dims, r=r or None, i=i, **extra_flags)


@bart_op
def ones(
    dims: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Create an array filled with ones with {dims} dimensions of size {dim1} to
    {dimn}.

    Equivalent to calling ``bart ones`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    dims : int
        Positional argument ``dims``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``ones`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("ones", [], output_dims, dims=dims, **extra_flags)


@bart_op
def pattern(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    s: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute sampling pattern from kspace

    Equivalent to calling ``bart pattern`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    s : int | None
        BART flag ``-s bitmask``: Squash dimensions selected by bitmask Default
        ``None``.
    **extra_flags : Any
        Additional BART ``pattern`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("pattern", [kspace], output_dims, s=s, **extra_flags)


@bart_op
def phantom(
    *,
    output_dims: list[int] | None = None,
    k: bool = False,
    flag_3: bool = False,
    flag_2: bool = False,
    b: bool = False,
    x: int | None = None,
    s: int | None = None,
    t: str | None = None,
    r: int | None = None,
    S: int | None = None,
    N: int | None = None,
    g: int | None = None,
    rotation_angle: float | None = None,
    rotation_steps: int | None = None,
    ellipsoid_center: tuple[int, int, int] | None = None,
    ellipsoid_axes: tuple[float, float, float] | None = None,
    FILE: str | None = None,
    stl: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Image and k-space domain phantoms.

    Equivalent to calling ``bart phantom`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    k : bool
        BART flag ``-k``: k-space Default ``False``.
    flag_3 : bool
        BART flag ``-3``: 3D simulation Default ``False``.
    flag_2 : bool
        BART flag ``-2``: enforce 2D simulation Default ``False``.
    b : bool
        BART flag ``-b``: basis functions for geometry Default ``False``.
    x : int | None
        BART flag ``-x n``: dimensions in y and z Default ``None``.
    s : int | None
        BART flag ``-s nc``: nc sensitivities Default ``None``.
    t : str | None
        BART flag ``-t file``: trajectory Default ``None``.
    r : int | None
        BART flag ``-r``: random seed initialization. '0' uses the default
        seed. Default ``None``.
    S : int | None
        BART flag ``-S nc``: Output nc sensitivities Default ``None``.
    N : int | None
        BART flag ``-N num``: Random tubes phantom with num tubes Default
        ``None``.
    g : int | None
        BART flag ``-g n=1,2,3``: select geometry for object phantom Default
        ``None``.
    rotation_angle : float | None
        BART flag ``--rotation-angle [deg]``: Angle of rotation Default
        ``None``.
    rotation_steps : int | None
        BART flag ``--rotation-steps n``: Number of rotation steps Default
        ``None``.
    ellipsoid_center : tuple[int, int, int] | None
        BART flag ``--ellipsoid_center``: x,y,z center coordinates of
        ellipsoid. Default ``None``.
    ellipsoid_axes : tuple[float, float, float] | None
        BART flag ``--ellipsoid_axes``: Axes lengths of ellipsoid. Default
        ``None``.
    FILE : str | None
        BART flag ``--FILE name``: Arbitrary geometry based on multicfl file.
        Default ``None``.
    stl : str | None
        BART flag ``--stl file``: path to stl file Default ``None``.
    **extra_flags : Any
        Additional BART ``phantom`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "phantom",
        [],
        output_dims,
        k=k or None,
        flag_3=flag_3 or None,
        flag_2=flag_2 or None,
        b=b or None,
        x=x,
        s=s,
        t=t,
        r=r,
        S=S,
        N=N,
        g=g,
        rotation_angle=rotation_angle,
        rotation_steps=rotation_steps,
        ellipsoid_center=ellipsoid_center,
        ellipsoid_axes=ellipsoid_axes,
        FILE=FILE,
        stl=stl,
        **extra_flags,
    )


@bart_op
def phasepole(
    input__coils_per_singularity_positions_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    c: str | None = None,
    w: str | None = None,
    a: str | None = None,
    t: float | None = None,
    d: float | None = None,
    n: int | None = None,
    espirit: bool = False,
    x: tuple[int, int, int] | None = None,
    closing: float | None = None,
    center: tuple[float, float, float] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Detect and sample phase poles.

    Equivalent to calling ``bart phasepole`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input__coils_per_singularity_positions_ : torch.Tensor
        Input CFL array ``input (coils/singularity positions)``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    c : str | None
        BART flag ``-c <curl>``: output curl map Default ``None``.
    w : str | None
        BART flag ``-w <weighting>``: (output weighting map) Default ``None``.
    a : str | None
        BART flag ``-a <acurl>``: (output averaged curl map) Default ``None``.
    t : float | None
        BART flag ``-t thresh``: threshold for pole detection Default ``None``.
    d : float | None
        BART flag ``-d diameter``: diameter of curve integral to detect poles
        (in FoV) Default ``None``.
    n : int | None
        BART flag ``-n normal``: normal direction for 2D pole detection (0,1,2)
        Default ``None``.
    espirit : bool
        BART flag ``--espirit``: Use ESPIRiT mode (diameter=1, no closing)
        Default ``False``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: Explicitly specify image dimensions Default
        ``None``.
    closing : float | None
        BART flag ``--closing radius``: (radius of closing ball (in FoV))
        Default ``None``.
    center : tuple[float, float, float] | None
        BART flag ``--center x:y:z``: specify position of phase pole to sample
        Default ``None``.
    **extra_flags : Any
        Additional BART ``phasepole`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "phasepole",
        [input__coils_per_singularity_positions_],
        output_dims,
        c=c,
        w=w,
        a=a,
        t=t,
        d=d,
        n=n,
        espirit=espirit or None,
        x=x,
        closing=closing,
        center=center,
        **extra_flags,
    )


@bart_op
def pics(
    kspace: torch.Tensor,
    sensitivities: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    c: bool = False,
    n: bool = False,
    N: bool = False,
    g: bool = False,
    e: bool = False,
    H: bool = False,
    D: bool = False,
    F: bool = False,
    J: bool = False,
    S: bool = False,
    K: bool = False,
    M: bool = False,
    r: float | None = None,
    s: float | None = None,
    i: int | None = None,
    t: str | None = None,
    p: str | None = None,
    b: int | None = None,
    T: str | None = None,
    W: str | None = None,
    d: int | None = None,
    O: int | None = None,
    o: float | None = None,
    u: float | None = None,
    C: int | None = None,
    q: float | None = None,
    f: float | None = None,
    w: float | None = None,
    L: int | None = None,
    B: str | None = None,
    P: float | None = None,
    gpu_gridding: bool = False,
    precond: bool = False,
    adaptive_stepsize: bool = False,
    asl: bool = False,
    teasl: bool = False,
    U: bool = False,
    no_toeplitz: bool = False,
    fista_last: bool = False,
    ist_last: bool = False,
    theta: tuple[float, float] | None = None,
    alpha: tuple[float, float] | None = None,
    gamma: tuple[float, float] | None = None,
    shared_img_dims: int | None = None,
    psf_export: str | None = None,
    psf_import: str | None = None,
    wavelet: str | None = None,
    mpi: int | None = None,
    fista_pqr: tuple[float, float, float] | None = None,
    motion_field: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Parallel-imaging compressed-sensing reconstruction.

    Equivalent to calling ``bart pics`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    sensitivities : torch.Tensor
        Input CFL array ``sensitivities``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    c : bool
        BART flag ``-c``: real-value constraint Default ``False``.
    n : bool
        BART flag ``-n``: disable random wavelet cycle spinning Default
        ``False``.
    N : bool
        BART flag ``-N``: do fully overlapping LLR blocks Default ``False``.
    g : bool
        BART flag ``-g``: use GPU Default ``False``.
    e : bool
        BART flag ``-e``: Scale stepsize based on max. eigenvalue Default
        ``False``.
    H : bool
        BART flag ``-H``: (hogwild) Default ``False``.
    D : bool
        BART flag ``-D``: (ADMM dynamic step size) Default ``False``.
    F : bool
        BART flag ``-F``: (fast) Default ``False``.
    J : bool
        BART flag ``-J``: (ADMM residual balancing) Default ``False``.
    S : bool
        BART flag ``-S``: re-scale the image after reconstruction Default
        ``False``.
    K : bool
        BART flag ``-K``: randshift for NUFFT Default ``False``.
    M : bool
        BART flag ``-M``: Simultaneous Multi-Slice reconstruction Default
        ``False``.
    r : float | None
        BART flag ``-r lambda``: regularization parameter Default ``None``.
    s : float | None
        BART flag ``-s step``: iteration stepsize Default ``None``.
    i : int | None
        BART flag ``-i iter``: max. number of iterations Default ``None``.
    t : str | None
        BART flag ``-t file``: k-space trajectory Default ``None``.
    p : str | None
        BART flag ``-p file``: pattern or weights Default ``None``.
    b : int | None
        BART flag ``-b blk``: Lowrank block size Default ``None``.
    T : str | None
        BART flag ``-T file``: (truth file) Default ``None``.
    W : str | None
        BART flag ``-W <img>``: Warm start with <img> Default ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    O : int | None
        BART flag ``-O rwiter``: (reweighting) Default ``None``.
    o : float | None
        BART flag ``-o gamma``: (reweighting) Default ``None``.
    u : float | None
        BART flag ``-u rho``: ADMM rho Default ``None``.
    C : int | None
        BART flag ``-C iter``: ADMM max. CG iterations Default ``None``.
    q : float | None
        BART flag ``-q cclambda``: (cclambda) Default ``None``.
    f : float | None
        BART flag ``-f rfov``: restrict FOV Default ``None``.
    w : float | None
        BART flag ``-w``: inverse scaling of the data Default ``None``.
    L : int | None
        BART flag ``-L flags``: (batch-mode) Default ``None``.
    B : str | None
        BART flag ``-B file``: temporal (or other) basis Default ``None``.
    P : float | None
        BART flag ``-P eps``: Basis Pursuit formulation, || y- Ax ||_2 <= eps
        Default ``None``.
    gpu_gridding : bool
        BART flag ``--gpu-gridding``: use GPU for gridding Default ``False``.
    precond : bool
        BART flag ``--precond``: interpret weights as preconditioner Default
        ``False``.
    adaptive_stepsize : bool
        BART flag ``--adaptive-stepsize``: PRIDU adaptive step size Default
        ``False``.
    asl : bool
        BART flag ``--asl``: ASL reconstruction Default ``False``.
    teasl : bool
        BART flag ``--teasl``: Time-encoded ASL reconstruction Default
        ``False``.
    U : bool
        BART flag ``-U``: Use low-mem mode of the nuFFT Default ``False``.
    no_toeplitz : bool
        BART flag ``--no-toeplitz``: (Turn off Toeplitz mode of nuFFT) Default
        ``False``.
    fista_last : bool
        BART flag ``--fista_last``: (end iteration with call to data
        consistency) Default ``False``.
    ist_last : bool
        BART flag ``--ist_last``: end iteration with call to data consistency
        Default ``False``.
    theta : tuple[float, float] | None
        BART flag ``--theta theta1:theta2``: PWI weight for ASL reconstruction
        Default ``None``.
    alpha : tuple[float, float] | None
        BART flag ``--alpha alpha1:alpha0``: regularization parameter for TGV
        and ICTGV reconstruction Default ``None``.
    gamma : tuple[float, float] | None
        BART flag ``--gamma gamma1:gamma2``: regularization parameter for ICTV
        and ICTGV reconstruction Default ``None``.
    shared_img_dims : int | None
        BART flag ``--shared-img-dims flags``: deselect image dims with flags
        Default ``None``.
    psf_export : str | None
        BART flag ``--psf_export file``: Export PSF to file Default ``None``.
    psf_import : str | None
        BART flag ``--psf_import file``: Import PSF from file Default ``None``.
    wavelet : str | None
        BART flag ``--wavelet name``: wavelet type (haar,dau2,cdf44) Default
        ``None``.
    mpi : int | None
        BART flag ``--mpi flags``: (distribute over this dimensions with use of
        MPI) Default ``None``.
    fista_pqr : tuple[float, float, float] | None
        BART flag ``--fista_pqr p:q:r``: parameters for FISTA acceleration
        Default ``None``.
    motion_field : str | None
        BART flag ``--motion-field file``: motion field Default ``None``.
    **extra_flags : Any
        Additional BART ``pics`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "pics",
        [kspace, sensitivities],
        output_dims,
        c=c or None,
        n=n or None,
        N=N or None,
        g=g or None,
        e=e or None,
        H=H or None,
        D=D or None,
        F=F or None,
        J=J or None,
        S=S or None,
        K=K or None,
        M=M or None,
        r=r,
        s=s,
        i=i,
        t=t,
        p=p,
        b=b,
        T=T,
        W=W,
        d=d,
        O=O,
        o=o,
        u=u,
        C=C,
        q=q,
        f=f,
        w=w,
        L=L,
        B=B,
        P=P,
        gpu_gridding=gpu_gridding or None,
        precond=precond or None,
        adaptive_stepsize=adaptive_stepsize or None,
        asl=asl or None,
        teasl=teasl or None,
        U=U or None,
        no_toeplitz=no_toeplitz or None,
        fista_last=fista_last or None,
        ist_last=ist_last or None,
        theta=theta,
        alpha=alpha,
        gamma=gamma,
        shared_img_dims=shared_img_dims,
        psf_export=psf_export,
        psf_import=psf_import,
        wavelet=wavelet,
        mpi=mpi,
        fista_pqr=fista_pqr,
        motion_field=motion_field,
        **extra_flags,
    )


@bart_op
def pocsense(
    kspace: torch.Tensor,
    sensitivities: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    i: int | None = None,
    r: float | None = None,
    l: int | None = None,
    o: float | None = None,
    m: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform POCSENSE reconstruction.

    Equivalent to calling ``bart pocsense`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    sensitivities : torch.Tensor
        Input CFL array ``sensitivities``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: () Default ``False``.
    i : int | None
        BART flag ``-i iter``: max. number of iterations Default ``None``.
    r : float | None
        BART flag ``-r alpha``: regularization parameter Default ``None``.
    l : int | None
        BART flag ``-l 1/-l2``: toggle l1-wavelet or l2 regularization Default
        ``None``.
    o : float | None
        BART flag ``-o``: () Default ``None``.
    m : float | None
        BART flag ``-m``: () Default ``None``.
    **extra_flags : Any
        Additional BART ``pocsense`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "pocsense",
        [kspace, sensitivities],
        output_dims,
        g=g or None,
        i=i,
        r=r,
        l=l,
        o=o,
        m=m,
        **extra_flags,
    )


@bart_op
def poisson(
    *,
    output_dims: list[int] | None = None,
    v: bool = False,
    e: bool = False,
    m: bool = False,
    Y: int | None = None,
    Z: int | None = None,
    y: float | None = None,
    z: float | None = None,
    C: int | None = None,
    V: float | None = None,
    D: float | None = None,
    T: int | None = None,
    R: int | None = None,
    s: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Computes Poisson-disc sampling pattern.

    Equivalent to calling ``bart poisson`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    v : bool
        BART flag ``-v``: variable density Default ``False``.
    e : bool
        BART flag ``-e``: elliptical scanning Default ``False``.
    m : bool
        BART flag ``-m``: () Default ``False``.
    Y : int | None
        BART flag ``-Y size``: size dimension 1 Default ``None``.
    Z : int | None
        BART flag ``-Z size``: size dimension 2 Default ``None``.
    y : float | None
        BART flag ``-y acc``: acceleration dim 1 Default ``None``.
    z : float | None
        BART flag ``-z acc``: acceleration dim 2 Default ``None``.
    C : int | None
        BART flag ``-C size``: size of calibration region Default ``None``.
    V : float | None
        BART flag ``-V``: (variable density) Default ``None``.
    D : float | None
        BART flag ``-D``: () Default ``None``.
    T : int | None
        BART flag ``-T``: () Default ``None``.
    R : int | None
        BART flag ``-R``: () Default ``None``.
    s : int | None
        BART flag ``-s``: random seed initialization. '0' uses the default
        seed. Default ``None``.
    **extra_flags : Any
        Additional BART ``poisson`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "poisson",
        [],
        output_dims,
        v=v or None,
        e=e or None,
        m=m or None,
        Y=Y,
        Z=Z,
        y=y,
        z=z,
        C=C,
        V=V,
        D=D,
        T=T,
        R=R,
        s=s,
        **extra_flags,
    )


@bart_op
def pol2mask(
    poly: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    X: int | None = None,
    Y: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute masks from polygons.

    Equivalent to calling ``bart pol2mask`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    poly : torch.Tensor
        Input CFL array ``poly``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    X : int | None
        BART flag ``-X size``: size dimension 0 Default ``None``.
    Y : int | None
        BART flag ``-Y size``: size dimension 1 Default ``None``.
    **extra_flags : Any
        Additional BART ``pol2mask`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("pol2mask", [poly], output_dims, X=X, Y=Y, **extra_flags)


@bart_op
def poly(
    L: int,
    N: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Evaluate polynomial p(x) = a_1 + a_2 x + a_3 x^2 ... a_(N+1) x^N at x = {0,
    1, ... , L - 1} where a_i are floats.

    Equivalent to calling ``bart poly`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    L : int
        Positional argument ``L``.
    N : int
        Positional argument ``N``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``poly`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("poly", [], output_dims, L=L, N=N, **extra_flags)


@bart_op
def psf(
    trajectory: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    oversampled: bool = False,
    oversampled_decomposed: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Calculate point-spread-function (PSF) of given trajectory.

    Equivalent to calling ``bart psf`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    trajectory : torch.Tensor
        Input CFL array ``trajectory``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    oversampled : bool
        BART flag ``--oversampled``: oversampled psf with linphases in dim 15
        Default ``False``.
    oversampled_decomposed : bool
        BART flag ``--oversampled-decomposed``: oversampled psf with linphases
        in dim 15 Default ``False``.
    **extra_flags : Any
        Additional BART ``psf`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "psf",
        [trajectory],
        output_dims,
        oversampled=oversampled or None,
        oversampled_decomposed=oversampled_decomposed or None,
        **extra_flags,
    )


@bart_op
def pulse(
    *,
    output_dims: list[int] | None = None,
    dur: float | None = None,
    fa: float | None = None,
    bwtp: float | None = None,
    mb: int | None = None,
    sms_dist: float | None = None,
    slice_th: float | None = None,
    N: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Pulse generation tool

    Equivalent to calling ``bart pulse`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    dur : float | None
        BART flag ``--dur double``: pulse duration [s] Default ``None``.
    fa : float | None
        BART flag ``--fa double``: flip angle [deg] Default ``None``.
    bwtp : float | None
        BART flag ``--bwtp double``: bandwidth time product Default ``None``.
    mb : int | None
        BART flag ``--mb long``: SMS multi-band factor Default ``None``.
    sms_dist : float | None
        BART flag ``--sms-dist long``: center-to-center slice distance between
        SMS partitions [m] Default ``None``.
    slice_th : float | None
        BART flag ``--slice-th double``: slice thickness [m] Default ``None``.
    N : int | None
        BART flag ``-N int``: number of time-steps (default = 1e6 * dur)
        Default ``None``.
    **extra_flags : Any
        Additional BART ``pulse`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "pulse",
        [],
        output_dims,
        dur=dur,
        fa=fa,
        bwtp=bwtp,
        mb=mb,
        sms_dist=sms_dist,
        slice_th=slice_th,
        N=N,
        **extra_flags,
    )


@bart_op
def raga(
    spokes: int,
    *,
    output_dims: list[int] | None = None,
    no_double_base: bool = False,
    s: int | None = None,
    r: int | None = None,
    e: int | None = None,
    m: int | None = None,
    z: int | None = None,
    i: int | None = None,
    c: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Generate file with RAGA indices for given approximated tiny golden ratio
    angle/raga increment and full frame spokes.

    Equivalent to calling ``bart raga`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    spokes : int
        Positional argument ``spokes``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    no_double_base : bool
        BART flag ``--no-double-base``: Define GA over Pi base instead of 2Pi.
        Default ``False``.
    s : int | None
        BART flag ``-s # Tiny GA``: tiny (small) golden ratio angle Default
        ``None``.
    r : int | None
        BART flag ``-r d``: Increment of RAGA Sampling Default ``None``.
    e : int | None
        BART flag ``-e m``: Number of (non-aligned) echoes Default ``None``.
    m : int | None
        BART flag ``-m m``: Number of (non-aligned) slices Default ``None``.
    z : int | None
        BART flag ``-z m``: Number of (non-aligned) partitions Default
        ``None``.
    i : int | None
        BART flag ``-i i``: Number of (non-aligned) inversions Default
        ``None``.
    c : int | None
        BART flag ``-c c``: Number of (non-aligned) shifts Default ``None``.
    **extra_flags : Any
        Additional BART ``raga`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "raga",
        [],
        output_dims,
        no_double_base=no_double_base or None,
        s=s,
        r=r,
        e=e,
        m=m,
        z=z,
        i=i,
        c=c,
        spokes=spokes,
        **extra_flags,
    )


@bart_op
def reconet(
    kspace: torch.Tensor,
    sensitivities: torch.Tensor,
    weights: torch.Tensor,
    ref_per_out: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    B: str | None = None,
    t: bool = False,
    e: bool = False,
    a: bool = False,
    g: bool = False,
    n: bool = False,
    rss_norm: bool = False,
    export: bool = False,
    load_memory: bool = False,
    lowmem: bool = False,
    test: bool = False,
    ksp_training: bool = False,
    no_precomp: bool = False,
    l: str | None = None,
    b: int | None = None,
    I: int | None = None,
    trajectory: str | None = None,
    pattern: str | None = None,
    adjoint: str | None = None,
    psf: str | None = None,
    mask: str | None = None,
    export_graph: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Trains or applies a neural network for reconstruction.

    Equivalent to calling ``bart reconet`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    sensitivities : torch.Tensor
        Input CFL array ``sensitivities``.
    weights : torch.Tensor
        Input CFL array ``weights``.
    ref_per_out : torch.Tensor
        Input CFL array ``ref/out``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    B : str | None
        BART flag ``-B file``: (temporal (or other) basis) Default ``None``.
    t : bool
        BART flag ``-t``: train reconet Default ``False``.
    e : bool
        BART flag ``-e``: evaluate reconet Default ``False``.
    a : bool
        BART flag ``-a``: apply reconet Default ``False``.
    g : bool
        BART flag ``-g``: run on gpu Default ``False``.
    n : bool
        BART flag ``-n``: normalize data with maximum magnitude of adjoint
        reconstruction Default ``False``.
    rss_norm : bool
        BART flag ``--rss-norm``: scale output image to rss normalization
        Default ``False``.
    export : bool
        BART flag ``--export``: (export psf and adjoint reconstruction) Default
        ``False``.
    load_memory : bool
        BART flag ``--load-memory``: copy training data into memory Default
        ``False``.
    lowmem : bool
        BART flag ``--lowmem``: reduce memory usage by checkpointing Default
        ``False``.
    test : bool
        BART flag ``--test``: very small network for tests Default ``False``.
    ksp_training : bool
        BART flag ``--ksp-training``: Train network on k-space data Default
        ``False``.
    no_precomp : bool
        BART flag ``--no-precomp``: Don't precompute adjoint and psf Default
        ``False``.
    l : str | None
        BART flag ``-l <weights-init>``: load weights for continuing training
        Default ``None``.
    b : int | None
        BART flag ``-b``: size of mini batches Default ``None``.
    I : int | None
        BART flag ``-I``: number of unrolled iterations Default ``None``.
    trajectory : str | None
        BART flag ``--trajectory <traj>``: trajectory Default ``None``.
    pattern : str | None
        BART flag ``--pattern <pattern>``: sampling pattern / psf in kspace
        Default ``None``.
    adjoint : str | None
        BART flag ``--adjoint <file>``: (validation data adjoint (load or
        export)) Default ``None``.
    psf : str | None
        BART flag ``--psf <file>``: (psf (load or export)) Default ``None``.
    mask : str | None
        BART flag ``--mask <mask>``: mask for computation of loss Default
        ``None``.
    export_graph : str | None
        BART flag ``--export-graph <file.dot>``: export graph for visualization
        Default ``None``.
    **extra_flags : Any
        Additional BART ``reconet`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "reconet",
        [kspace, sensitivities, weights, ref_per_out],
        output_dims,
        B=B,
        t=t or None,
        e=e or None,
        a=a or None,
        g=g or None,
        n=n or None,
        rss_norm=rss_norm or None,
        export=export or None,
        load_memory=load_memory or None,
        lowmem=lowmem or None,
        test=test or None,
        ksp_training=ksp_training or None,
        no_precomp=no_precomp or None,
        l=l,
        b=b,
        I=I,
        trajectory=trajectory,
        pattern=pattern,
        adjoint=adjoint,
        psf=psf,
        mask=mask,
        export_graph=export_graph,
        **extra_flags,
    )


@bart_op
def repmat(
    input_: torch.Tensor,
    dimension: int,
    repetitions: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Repeat input array multiple times along a certain dimension.

    Equivalent to calling ``bart repmat`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dimension : int
        Positional argument ``dimension``.
    repetitions : int
        Positional argument ``repetitions``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``repmat`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "repmat", [input_], output_dims, dimension=dimension, repetitions=repetitions, **extra_flags
    )


@bart_op
def reshape(
    input_: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    s: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Reshape selected dimensions.

    Equivalent to calling ``bart reshape`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    s : int | None
        BART flag ``-s flags``: stream flagged dims Default ``None``.
    **extra_flags : Any
        Additional BART ``reshape`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("reshape", [input_], output_dims, s=s, flags=flags, **extra_flags)


@bart_op
def resize(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Resizes an array along dimensions to sizes by truncating or zero-padding.
    Please see doc/resize.txt for examples.

    Equivalent to calling ``bart resize`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``resize`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("resize", [input_], output_dims, **extra_flags)


@bart_op
def rmfreq(
    traj: torch.Tensor,
    k: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    N: int | None = None,
    M: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Remove angle-dependent frequency

    Equivalent to calling ``bart rmfreq`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    traj : torch.Tensor
        Input CFL array ``traj``.
    k : torch.Tensor
        Input CFL array ``k``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    N : int | None
        BART flag ``-N #``: Number of harmonics [Default: 5] Default ``None``.
    M : str | None
        BART flag ``-M file``: Contrast modulation file Default ``None``.
    **extra_flags : Any
        Additional BART ``rmfreq`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("rmfreq", [traj, k], output_dims, N=N, M=M, **extra_flags)


@bart_op
def rof(
    input_: torch.Tensor,
    lambda_: float,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform total variation denoising along dims <flags>.

    Equivalent to calling ``bart rof`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    lambda_ : float
        Positional argument ``lambda``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``rof`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("rof", [input_], output_dims, lambda_=lambda_, flags=flags, **extra_flags)


@bart_op
def roistat(
    roi: torch.Tensor,
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    b: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute ROI statistics.

    Equivalent to calling ``bart roistat`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    roi : torch.Tensor
        Input CFL array ``roi``.
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    b : bool
        BART flag ``-b``: Bessel's correction, i.e. 1 / (n - 1) Default
        ``False``.
    **extra_flags : Any
        Additional BART ``roistat`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("roistat", [roi, input_], output_dims, b=b or None, **extra_flags)


@bart_op
def rovir(
    positive_signal: torch.Tensor,
    negative_negative: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute coil compression matrix using ROVir.

    Equivalent to calling ``bart rovir`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    positive_signal : torch.Tensor
        Input CFL array ``positive signal``.
    negative_negative : torch.Tensor
        Input CFL array ``negative negative``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``rovir`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("rovir", [positive_signal, negative_negative], output_dims, **extra_flags)


@bart_op
def rss(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Calculates root of sum of squares along selected dimensions.

    Equivalent to calling ``bart rss`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``rss`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("rss", [input_], output_dims, bitmask=bitmask, **extra_flags)


@bart_op
def rtnlinv(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    c: bool = False,
    N: bool = False,
    U: bool = False,
    g: bool = False,
    S: bool = False,
    A: bool = False,
    s: bool = False,
    i: int | None = None,
    R: float | None = None,
    M: float | None = None,
    d: int | None = None,
    m: int | None = None,
    f: float | None = None,
    p: str | None = None,
    t: str | None = None,
    I: str | None = None,
    C: str | None = None,
    a: float | None = None,
    b: float | None = None,
    T: float | None = None,
    w: float | None = None,
    x: tuple[int, int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Jointly estimate a time-series of images and sensitivities with nonlinear

    Equivalent to calling ``bart rtnlinv`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    c : bool
        BART flag ``-c``: Real-value constraint Default ``False``.
    N : bool
        BART flag ``-N``: Do not normalize image with coil sensitivities
        Default ``False``.
    U : bool
        BART flag ``-U``: Do not combine ENLIVE maps in output Default
        ``False``.
    g : bool
        BART flag ``-g``: use gpu Default ``False``.
    S : bool
        BART flag ``-S``: Re-scale image after reconstruction Default
        ``False``.
    A : bool
        BART flag ``-A``: (Alternative scaling) Default ``False``.
    s : bool
        BART flag ``-s``: (Simultaneous Multi-Slice reconstruction) Default
        ``False``.
    i : int | None
        BART flag ``-i iter``: Number of Newton steps Default ``None``.
    R : float | None
        BART flag ``-R``: (reduction factor) Default ``None``.
    M : float | None
        BART flag ``-M``: (minimum for regularization) Default ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    m : int | None
        BART flag ``-m nmaps``: Number of ENLIVE maps to use in reconstruction
        Default ``None``.
    f : float | None
        BART flag ``-f FOV``: restrict FOV Default ``None``.
    p : str | None
        BART flag ``-p file``: pattern / transfer function Default ``None``.
    t : str | None
        BART flag ``-t file``: kspace trajectory Default ``None``.
    I : str | None
        BART flag ``-I file``: File for initialization Default ``None``.
    C : str | None
        BART flag ``-C``: (File for initialization with image space
        sensitivities) Default ``None``.
    a : float | None
        BART flag ``-a``: (a in 1 + a * \\Laplace^-b/2) Default ``None``.
    b : float | None
        BART flag ``-b``: (b in 1 + a * \\Laplace^-b/2) Default ``None``.
    T : float | None
        BART flag ``-T temp_damp``: temporal damping [default: 0.9] Default
        ``None``.
    w : float | None
        BART flag ``-w``: (inverse scaling of the data) Default ``None``.
    x : tuple[int, int, int] | None
        BART flag ``-x x:y:z``: Explicitly specify image dimensions Default
        ``None``.
    **extra_flags : Any
        Additional BART ``rtnlinv`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "rtnlinv",
        [kspace],
        output_dims,
        c=c or None,
        N=N or None,
        U=U or None,
        g=g or None,
        S=S or None,
        A=A or None,
        s=s or None,
        i=i,
        R=R,
        M=M,
        d=d,
        m=m,
        f=f,
        p=p,
        t=t,
        I=I,
        C=C,
        a=a,
        b=b,
        T=T,
        w=w,
        x=x,
        **extra_flags,
    )


@bart_op
def sake(
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    i: int | None = None,
    s: float | None = None,
    o: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Use SAKE algorithm to recover a full k-space from undersampled

    Equivalent to calling ``bart sake`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    i : int | None
        BART flag ``-i iter``: number of iterations Default ``None``.
    s : float | None
        BART flag ``-s size``: rel. size of the signal subspace Default
        ``None``.
    o : float | None
        BART flag ``-o``: () Default ``None``.
    **extra_flags : Any
        Additional BART ``sake`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("sake", [kspace], output_dims, i=i, s=s, o=o, **extra_flags)


@bart_op
def sample(
    samples: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    s: int | None = None,
    N: int | None = None,
    K: int | None = None,
    S: int | None = None,
    r: bool = False,
    a: bool = False,
    p: bool = False,
    external_graph: str | None = None,
    mask: str | None = None,
    gamma: float | None = None,
    save_mod: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Prior sampling with given diffusion network (either PyTorch or TensorFlow)
    which is

    Equivalent to calling ``bart sample`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    samples : torch.Tensor
        Input CFL array ``samples``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: use gpu Default ``False``.
    s : int | None
        BART flag ``-s s``: seed Default ``None``.
    N : int | None
        BART flag ``-N N``: number of noise levels Default ``None``.
    K : int | None
        BART flag ``-K K``: number of Langevin steps per level Default
        ``None``.
    S : int | None
        BART flag ``-S S``: number of samples drawn Default ``None``.
    r : bool
        BART flag ``-r``: real-valued trained network (i.e. with z ~ CN(0, 2I))
        Default ``False``.
    a : bool
        BART flag ``-a``: ancestral sampling Default ``False``.
    p : bool
        BART flag ``-p``: predictor-corrector sampling Default ``False``.
    external_graph : str | None
        BART flag ``--external-graph weights``: .pt or .tf file with weights
        Default ``None``.
    mask : str | None
        BART flag ``--mask file``: FoV mask for output of network Default
        ``None``.
    gamma : float | None
        BART flag ``--gamma gamma``: scaling of stepsize for Langevin iteration
        Default ``None``.
    save_mod : int | None
        BART flag ``--save-mod S``: save samples every S steps Default
        ``None``.
    **extra_flags : Any
        Additional BART ``sample`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "sample",
        [samples],
        output_dims,
        g=g or None,
        s=s,
        N=N,
        K=K,
        S=S,
        r=r or None,
        a=a or None,
        p=p or None,
        external_graph=external_graph,
        mask=mask,
        gamma=gamma,
        save_mod=save_mod,
        **extra_flags,
    )


@bart_op
def saxpy(
    scale: torch.Tensor,
    input1: torch.Tensor,
    input2: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Multiply input1 with scale factor and add input2.

    Equivalent to calling ``bart saxpy`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    scale : torch.Tensor
        Input CFL array ``scale``.
    input1 : torch.Tensor
        Input CFL array ``input1``.
    input2 : torch.Tensor
        Input CFL array ``input2``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``saxpy`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("saxpy", [scale, input1, input2], output_dims, **extra_flags)


@bart_op
def scale(
    factor: torch.Tensor,
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Scale array by {factor}. The scale factor can be a complex number.

    Equivalent to calling ``bart scale`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    factor : torch.Tensor
        Input CFL array ``factor``.
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``scale`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("scale", [factor, input_], output_dims, **extra_flags)


@bart_op
def sdot(
    input1: torch.Tensor,
    input2: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute dot product along selected dimensions.

    Equivalent to calling ``bart sdot`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input1 : torch.Tensor
        Input CFL array ``input1``.
    input2 : torch.Tensor
        Input CFL array ``input2``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``sdot`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("sdot", [input1, input2], output_dims, **extra_flags)


@bart_op
def seq(
    *,
    output_dims: list[int] | None = None,
    d: float | None = None,
    N: int | None = None,
    R: str | None = None,
    chrono: bool = False,
    dist: float | None = None,
    FOV: float | None = None,
    BR: int | None = None,
    slice_thickness: float | None = None,
    FA: float | None = None,
    TR: float | None = None,
    TE: float | None = None,
    TE_delta: float | None = None,
    BWTP: float | None = None,
    rf_duration: float | None = None,
    dwell: float | None = None,
    os: float | None = None,
    pe_mode: int | None = None,
    raga_flags: int | None = None,
    tiny: int | None = None,
    e: int | None = None,
    r: int | None = None,
    z: int | None = None,
    t: int | None = None,
    f: int | None = None,
    m: int | None = None,
    i: int | None = None,
    mb_factor: int | None = None,
    sms_distance: float | None = None,
    TI: float | None = None,
    init_delay: float | None = None,
    inv_delay: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Computes a GRE sequence.

    Equivalent to calling ``bart seq`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    d : float | None
        BART flag ``-d dt``: time-increment per sample (default:
        seq->conf->phys.tr / 1000) Default ``None``.
    N : int | None
        BART flag ``-N samples``: Number of samples (default: 1000) Default
        ``None``.
    R : str | None
        BART flag ``-R file``: raga indices Default ``None``.
    chrono : bool
        BART flag ``--chrono``: save gradients/moments/sampling in
        chronological order (RAGA) Default ``False``.
    dist : float | None
        BART flag ``--dist dist``: slice distance factor [1 / slice_thickness]
        (default: 1.) Default ``None``.
    FOV : float | None
        BART flag ``--FOV FOV``: Field Of View Default ``None``.
    BR : int | None
        BART flag ``--BR BR``: Base Resolution Default ``None``.
    slice_thickness : float | None
        BART flag ``--slice_thickness slice_thickness``: Slice thickness
        Default ``None``.
    FA : float | None
        BART flag ``--FA flip angle``: Flip angle [deg] Default ``None``.
    TR : float | None
        BART flag ``--TR TR``: TR Default ``None``.
    TE : float | None
        BART flag ``--TE TE``: TE Default ``None``.
    TE_delta : float | None
        BART flag ``--TE_delta TE_delta``: TE_delta Default ``None``.
    BWTP : float | None
        BART flag ``--BWTP BWTP``: Bandwidth Time Product Default ``None``.
    rf_duration : float | None
        BART flag ``--rf_duration rf_duration``: RF pulse duration Default
        ``None``.
    dwell : float | None
        BART flag ``--dwell dwell``: Dwell time Default ``None``.
    os : float | None
        BART flag ``--os os``: Oversampling factor Default ``None``.
    pe_mode : int | None
        BART flag ``--pe_mode pe_mode``: Phase-encoding mode Default ``None``.
    raga_flags : int | None
        BART flag ``--raga_flags raga_aligned_flags``: RAGA aligned flags (by
        bitmask) Default ``None``.
    tiny : int | None
        BART flag ``--tiny tiny``: Tiny golden-ratio index Default ``None``.
    e : int | None
        BART flag ``-e echoes``: Number of echoes Default ``None``.
    r : int | None
        BART flag ``-r lines``: Number of phase encoding lines Default
        ``None``.
    z : int | None
        BART flag ``-z partitions``: Number of partitions (3D) or SMS groups
        (2D) Default ``None``.
    t : int | None
        BART flag ``-t measurements``: Number of measurements / frames (RAGA:
        total number of spokes) Default ``None``.
    f : int | None
        BART flag ``-f raga_full_frames``: Number of full frames (only RAGA)
        Default ``None``.
    m : int | None
        BART flag ``-m slices``: Number of slices of multiband factor (SMS)
        Default ``None``.
    i : int | None
        BART flag ``-i inversions``: Number of inversions Default ``None``.
    mb_factor : int | None
        BART flag ``--mb_factor mb_factor``: Multi-band factor Default
        ``None``.
    sms_distance : float | None
        BART flag ``--sms_distance sms_distance``: SMS slice distance Default
        ``None``.
    TI : float | None
        BART flag ``--TI TI``: Inversion time Default ``None``.
    init_delay : float | None
        BART flag ``--init_delay init_delay``: Initial delay of measurement
        Default ``None``.
    inv_delay : float | None
        BART flag ``--inv_delay inv_delay_time``: Inversion delay time Default
        ``None``.
    **extra_flags : Any
        Additional BART ``seq`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "seq",
        [],
        output_dims,
        d=d,
        N=N,
        R=R,
        chrono=chrono or None,
        dist=dist,
        FOV=FOV,
        BR=BR,
        slice_thickness=slice_thickness,
        FA=FA,
        TR=TR,
        TE=TE,
        TE_delta=TE_delta,
        BWTP=BWTP,
        rf_duration=rf_duration,
        dwell=dwell,
        os=os,
        pe_mode=pe_mode,
        raga_flags=raga_flags,
        tiny=tiny,
        e=e,
        r=r,
        z=z,
        t=t,
        f=f,
        m=m,
        i=i,
        mb_factor=mb_factor,
        sms_distance=sms_distance,
        TI=TI,
        init_delay=init_delay,
        inv_delay=inv_delay,
        **extra_flags,
    )


@bart_op
def show(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    m: bool = False,
    R: bool = False,
    N: bool = False,
    d: int | None = None,
    s: str | None = None,
    f: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Outputs values or meta data.

    Equivalent to calling ``bart show`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    m : bool
        BART flag ``-m``: show meta data Default ``False``.
    R : bool
        BART flag ``-R``: print only real part Default ``False``.
    N : bool
        BART flag ``-N``: print index Default ``False``.
    d : int | None
        BART flag ``-d dim``: show size of dimension Default ``None``.
    s : str | None
        BART flag ``-s sep``: use <sep> as the separator Default ``None``.
    f : str | None
        BART flag ``-f format``: use <format> as the format. Default:
        \\"%+.6e%+.6ei\\" Default ``None``.
    **extra_flags : Any
        Additional BART ``show`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "show",
        [input_],
        output_dims,
        m=m or None,
        R=R or None,
        N=N or None,
        d=d,
        s=s,
        f=f,
        **extra_flags,
    )


@bart_op
def signal(
    *,
    output_dims: list[int] | None = None,
    I: bool = False,
    s: bool = False,
    flag_0: tuple[float, float, float] | None = None,
    flag_1: tuple[float, float, float] | None = None,
    flag_2: tuple[float, float, float] | None = None,
    flag_3: tuple[float, float, float] | None = None,
    flag_4: tuple[float, float, float] | None = None,
    flag_5: tuple[float, float, float] | None = None,
    flag_6: tuple[float, float, float] | None = None,
    r: float | None = None,
    e: float | None = None,
    i: float | None = None,
    f: float | None = None,
    d: float | None = None,
    t: float | None = None,
    n: int | None = None,
    b: int | None = None,
    m: int | None = None,
    l: float | None = None,
    a: float | None = None,
    fat: bool = False,
    short_TR_LL_approx: bool = False,
    pulsed: bool = False,
    acquisition_only: bool = False,
    av_spokes: int | None = None,
    freq_samples: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Analytical simulation tool.

    Equivalent to calling ``bart signal`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    I : bool
        BART flag ``-I``: inversion recovery Default ``False``.
    s : bool
        BART flag ``-s``: inversion recovery starting from steady state Default
        ``False``.
    flag_0 : tuple[float, float, float] | None
        BART flag ``-0 min:max:N``: range of off-resonance frequency [Hz]
        Default ``None``.
    flag_1 : tuple[float, float, float] | None
        BART flag ``-1 min:max:N``: range of T1s [s] Default ``None``.
    flag_2 : tuple[float, float, float] | None
        BART flag ``-2 min:max:N``: range of T2s [s] Default ``None``.
    flag_3 : tuple[float, float, float] | None
        BART flag ``-3 min:max:N``: range of Mss Default ``None``.
    flag_4 : tuple[float, float, float] | None
        BART flag ``-4 min:max:N``: range of T1 values for fat [s] Default
        ``None``.
    flag_5 : tuple[float, float, float] | None
        BART flag ``-5 min:max:N``: range of FA values [°] Default ``None``.
    flag_6 : tuple[float, float, float] | None
        BART flag ``-6 min:max:N``: range of CBF values [ml/100g/min] Default
        ``None``.
    r : float | None
        BART flag ``-r TR``: repetition time Default ``None``.
    e : float | None
        BART flag ``-e TE``: echo time Default ``None``.
    i : float | None
        BART flag ``-i TI``: inversion time Default ``None``.
    f : float | None
        BART flag ``-f FA``: flip angle Default ``None``.
    d : float | None
        BART flag ``-d FF``: fat fraction Default ``None``.
    t : float | None
        BART flag ``-t T1 relax``: T1 relax period (second) for MOLLI Default
        ``None``.
    n : int | None
        BART flag ``-n n``: number of measurements Default ``None``.
    b : int | None
        BART flag ``-b heart beats``: number of heart beats for MOLLI Default
        ``None``.
    m : int | None
        BART flag ``-m multi echos``: number of multi gradient echos Default
        ``None``.
    l : float | None
        BART flag ``-l lambda``: Blood-brain partition coefficient Default
        ``None``.
    a : float | None
        BART flag ``-a Delta t``: Arterial transit time (ATT) in Buxton model
        Default ``None``.
    fat : bool
        BART flag ``--fat``: Simulate additional fat component. Default
        ``False``.
    short_TR_LL_approx : bool
        BART flag ``--short-TR-LL-approx``: Short TR approximation for
        analytical LL model. Default ``False``.
    pulsed : bool
        BART flag ``--pulsed``: Pulsed Arterial Spin Labeling Default
        ``False``.
    acquisition_only : bool
        BART flag ``--acquisition-only``: only return the Buxton model signal
        during image acquisition Default ``False``.
    av_spokes : int | None
        BART flag ``--av-spokes``: Number of averaged consecutive spokes
        Default ``None``.
    freq_samples : int | None
        BART flag ``--freq-samples``: Samples in frequency-/z-domain for FSE
        model based on generating function formalism Default ``None``.
    **extra_flags : Any
        Additional BART ``signal`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "signal",
        [],
        output_dims,
        I=I or None,
        s=s or None,
        flag_0=flag_0,
        flag_1=flag_1,
        flag_2=flag_2,
        flag_3=flag_3,
        flag_4=flag_4,
        flag_5=flag_5,
        flag_6=flag_6,
        r=r,
        e=e,
        i=i,
        f=f,
        d=d,
        t=t,
        n=n,
        b=b,
        m=m,
        l=l,
        a=a,
        fat=fat or None,
        short_TR_LL_approx=short_TR_LL_approx or None,
        pulsed=pulsed or None,
        acquisition_only=acquisition_only or None,
        av_spokes=av_spokes,
        freq_samples=freq_samples,
        **extra_flags,
    )


@bart_op
def sim(
    *,
    output_dims: list[int] | None = None,
    split_dim: bool = False,
    flag_1: tuple[float, float, float] | None = None,
    flag_2: tuple[float, float, float] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    simulation tool

    Equivalent to calling ``bart sim`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    split_dim : bool
        BART flag ``--split-dim``: split magnetization into x, y, and z
        component Default ``False``.
    flag_1 : tuple[float, float, float] | None
        BART flag ``-1 min:max:N``: range of T1 values Default ``None``.
    flag_2 : tuple[float, float, float] | None
        BART flag ``-2 min:max:N``: range of T2 values Default ``None``.
    **extra_flags : Any
        Additional BART ``sim`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "sim",
        [],
        output_dims,
        split_dim=split_dim or None,
        flag_1=flag_1,
        flag_2=flag_2,
        **extra_flags,
    )


@bart_op
def slice(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Extracts a slice from positions along dimensions.

    Equivalent to calling ``bart slice`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``slice`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("slice", [input_], output_dims, **extra_flags)


@bart_op
def spow(
    exponent: torch.Tensor,
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Raise array to the power of {exponent}. The exponent can be a complex
    number.

    Equivalent to calling ``bart spow`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    exponent : torch.Tensor
        Input CFL array ``exponent``.
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``spow`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("spow", [exponent, input_], output_dims, **extra_flags)


@bart_op
def sqpics(
    kspace: torch.Tensor,
    sensitivities: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    n: bool = False,
    g: bool = False,
    e: bool = False,
    H: bool = False,
    F: bool = False,
    S: bool = False,
    r: float | None = None,
    s: float | None = None,
    i: int | None = None,
    t: str | None = None,
    p: str | None = None,
    b: int | None = None,
    T: str | None = None,
    W: str | None = None,
    d: int | None = None,
    u: float | None = None,
    C: int | None = None,
    f: float | None = None,
    w: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Parallel-imaging compressed-sensing reconstruction.

    Equivalent to calling ``bart sqpics`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    sensitivities : torch.Tensor
        Input CFL array ``sensitivities``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    n : bool
        BART flag ``-n``: disable random wavelet cycle spinning Default
        ``False``.
    g : bool
        BART flag ``-g``: use GPU Default ``False``.
    e : bool
        BART flag ``-e``: Scale stepsize based on max. eigenvalue Default
        ``False``.
    H : bool
        BART flag ``-H``: (hogwild) Default ``False``.
    F : bool
        BART flag ``-F``: (fast) Default ``False``.
    S : bool
        BART flag ``-S``: Re-scale the image after reconstruction Default
        ``False``.
    r : float | None
        BART flag ``-r lambda``: regularization parameter Default ``None``.
    s : float | None
        BART flag ``-s step``: iteration stepsize Default ``None``.
    i : int | None
        BART flag ``-i iter``: max. number of iterations Default ``None``.
    t : str | None
        BART flag ``-t file``: k-space trajectory Default ``None``.
    p : str | None
        BART flag ``-p file``: pattern or weights Default ``None``.
    b : int | None
        BART flag ``-b blk``: Lowrank block size Default ``None``.
    T : str | None
        BART flag ``-T file``: (truth file) Default ``None``.
    W : str | None
        BART flag ``-W <img>``: Warm start with <img> Default ``None``.
    d : int | None
        BART flag ``-d level``: Debug level Default ``None``.
    u : float | None
        BART flag ``-u rho``: ADMM rho Default ``None``.
    C : int | None
        BART flag ``-C iter``: ADMM max. CG iterations Default ``None``.
    f : float | None
        BART flag ``-f rfov``: restrict FOV Default ``None``.
    w : float | None
        BART flag ``-w val``: scaling Default ``None``.
    **extra_flags : Any
        Additional BART ``sqpics`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "sqpics",
        [kspace, sensitivities],
        output_dims,
        n=n or None,
        g=g or None,
        e=e or None,
        H=H or None,
        F=F or None,
        S=S or None,
        r=r,
        s=s,
        i=i,
        t=t,
        p=p,
        b=b,
        T=T,
        W=W,
        d=d,
        u=u,
        C=C,
        f=f,
        w=w,
        **extra_flags,
    )


@bart_op
def squeeze(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Remove singleton dimensions of array.

    Equivalent to calling ``bart squeeze`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``squeeze`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("squeeze", [input_], output_dims, **extra_flags)


@bart_op
def ssa(
    src: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    z: bool = False,
    w: int | None = None,
    m: int | None = None,
    n: int | None = None,
    r: int | None = None,
    g: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform SSA-FARY or Singular Spectrum Analysis. <src>: [samples,
    coordinates]

    Equivalent to calling ``bart ssa`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    src : torch.Tensor
        Input CFL array ``src``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    z : bool
        BART flag ``-z``: Zeropadding [Default: True] Default ``False``.
    w : int | None
        BART flag ``-w window``: Window length Default ``None``.
    m : int | None
        BART flag ``-m 0/1``: Remove mean [Default: True] Default ``None``.
    n : int | None
        BART flag ``-n 0/1``: Normalize [Default: False] Default ``None``.
    r : int | None
        BART flag ``-r rank``: Rank for backprojection. r < 0: Throw away first
        r components. r > 0: Use only first r components. Default ``None``.
    g : int | None
        BART flag ``-g bitmask``: Bitmask for Grouping (long value!) Default
        ``None``.
    **extra_flags : Any
        Additional BART ``ssa`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("ssa", [src], output_dims, z=z or None, w=w, m=m, n=n, r=r, g=g, **extra_flags)


@bart_op
def std(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute standard deviation along selected dimensions specified by the
    {bitmask}

    Equivalent to calling ``bart std`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``std`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("std", [input_], output_dims, bitmask=bitmask, **extra_flags)


@bart_op
def stl(
    *,
    output_dims: list[int] | None = None,
    s: float | None = None,
    m: tuple[float, float, float] | None = None,
    stat: bool = False,
    binary: bool = False,
    no_nc: bool = False,
    input_: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Read and write stl files with '.stl' or cfl fileformat.

    Equivalent to calling ``bart stl`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    s : float | None
        BART flag ``-s scale``: Multiplicate all coordinates of model with a
        scale factor. Default ``None``.
    m : tuple[float, float, float] | None
        BART flag ``-m move``: Move model by vector. Default ``None``.
    stat : bool
        BART flag ``--stat``: Show statistics of model. Default ``False``.
    binary : bool
        BART flag ``--binary``: Output STL files in binary format. Default
        ``False``.
    no_nc : bool
        BART flag ``--no-nc``: (Don't recompute normal vectors with double
        precision.) Default ``False``.
    input_ : str | None
        BART flag ``--input``: Path to input file (.stl or cfl file format).
        Default ``None``.
    **extra_flags : Any
        Additional BART ``stl`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "stl",
        [],
        output_dims,
        s=s,
        m=m,
        stat=stat or None,
        binary=binary or None,
        no_nc=no_nc or None,
        input_=input_,
        **extra_flags,
    )


@bart_op
def svd(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    e: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute singular-value-decomposition (SVD).

    Equivalent to calling ``bart svd`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    e : bool
        BART flag ``-e``: econ Default ``False``.
    **extra_flags : Any
        Additional BART ``svd`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("svd", [input_], output_dims, e=e or None, **extra_flags)


@bart_op
def tee(
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Copy stdin to stdout + given output files.

    Equivalent to calling ``bart tee`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``tee`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("tee", [], output_dims, **extra_flags)


@bart_op
def tensorflow(
    TensorFlow_Graph: str,
    *,
    output_dims: list[int] | None = None,
    n: bool = False,
    g: bool = False,
    b: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Load Tensorflow Graph

    Equivalent to calling ``bart tensorflow`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    TensorFlow_Graph : str
        Positional argument ``TensorFlow Graph``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    n : bool
        BART flag ``-n``: Print all nodes in graph Default ``False``.
    g : bool
        BART flag ``-g``: Use gpu Default ``False``.
    b : int | None
        BART flag ``-b b``: Fill placeholder in dims with b Default ``None``.
    **extra_flags : Any
        Additional BART ``tensorflow`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "tensorflow",
        [],
        output_dims,
        n=n or None,
        g=g or None,
        b=b,
        TensorFlow_Graph=TensorFlow_Graph,
        **extra_flags,
    )


@bart_op
def tgv(
    input_: torch.Tensor,
    lambda_: float,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    alpha: tuple[float, float] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform total generalized variation denoising along dims specified by flags.

    Equivalent to calling ``bart tgv`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    lambda_ : float
        Positional argument ``lambda``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    alpha : tuple[float, float] | None
        BART flag ``--alpha alpha1:alpha0``: alpha1 * || grad x - z ||_1,
        alpha0 * || Eps z ||_1 Default ``None``.
    **extra_flags : Any
        Additional BART ``tgv`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "tgv", [input_], output_dims, alpha=alpha, lambda_=lambda_, flags=flags, **extra_flags
    )


@bart_op
def threshold(
    input_: torch.Tensor,
    lambda_: float,
    *,
    output_dims: list[int] | None = None,
    j: int | None = None,
    b: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform (soft) thresholding with parameter lambda.

    Equivalent to calling ``bart threshold`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    lambda_ : float
        Positional argument ``lambda``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    j : int | None
        BART flag ``-j bitmask``: joint soft-thresholding Default ``None``.
    b : int | None
        BART flag ``-b blocksize``: locally low rank block size Default
        ``None``.
    **extra_flags : Any
        Additional BART ``threshold`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("threshold", [input_], output_dims, j=j, b=b, lambda_=lambda_, **extra_flags)


@bart_op
def toimg(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    d: bool = False,
    m: bool = False,
    W: bool = False,
    D: bool = False,
    g: float | None = None,
    c: float | None = None,
    w: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Create magnitude images as png or proto-dicom.

    Equivalent to calling ``bart toimg`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    d : bool
        BART flag ``-d``: write to dicom format (deprecated, use extension
        .dcm) Default ``False``.
    m : bool
        BART flag ``-m``: re-scale each image Default ``False``.
    W : bool
        BART flag ``-W``: use dynamic windowing Default ``False``.
    D : bool
        BART flag ``-D``: Include dimensions in output filenames Default
        ``False``.
    g : float | None
        BART flag ``-g gamma``: gamma level Default ``None``.
    c : float | None
        BART flag ``-c contrast``: contrast level Default ``None``.
    w : float | None
        BART flag ``-w window``: window level Default ``None``.
    **extra_flags : Any
        Additional BART ``toimg`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "toimg",
        [input_],
        output_dims,
        d=d or None,
        m=m or None,
        W=W or None,
        D=D or None,
        g=g,
        c=c,
        w=w,
        **extra_flags,
    )


@bart_op
def toraw(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    n: bool = False,
    c: bool = False,
    b: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Output BART file as raw samples.

    Equivalent to calling ``bart toraw`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    n : bool
        BART flag ``-n``: Normalize input Default ``False``.
    c : bool
        BART flag ``-c``: Complex output Default ``False``.
    b : int | None
        BART flag ``-b bitwidth``: Number of bits (default: 8) Default
        ``None``.
    **extra_flags : Any
        Additional BART ``toraw`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("toraw", [input_], output_dims, n=n or None, c=c or None, b=b, **extra_flags)


@bart_op
def traj(
    *,
    output_dims: list[int] | None = None,
    l: bool = False,
    g: bool = False,
    r: bool = False,
    G: bool = False,
    H: bool = False,
    A: bool = False,
    D: bool = False,
    O: bool = False,
    flag_3: bool = False,
    c: bool = False,
    E: bool = False,
    x: int | None = None,
    y: int | None = None,
    z: int | None = None,
    d: int | None = None,
    e: int | None = None,
    a: int | None = None,
    t: int | None = None,
    m: int | None = None,
    s: int | None = None,
    o: float | None = None,
    R: float | None = None,
    q: tuple[float, float, float] | None = None,
    Q: tuple[float, float, float] | None = None,
    C: str | None = None,
    V: str | None = None,
    double_base: bool = False,
    mems_legacy: bool = False,
    z_us: tuple[int, int] | None = None,
    raga_inc: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Computes k-space trajectories.

    Equivalent to calling ``bart traj`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    l : bool
        BART flag ``-l``: aligned partition angle Default ``False``.
    g : bool
        BART flag ``-g``: (golden angle in partition direction) Default
        ``False``.
    r : bool
        BART flag ``-r``: radial Default ``False``.
    G : bool
        BART flag ``-G``: golden-ratio sampling Default ``False``.
    H : bool
        BART flag ``-H``: half-circle golden-ratio sampling Default ``False``.
    A : bool
        BART flag ``-A``: rational approximation of golden angles Default
        ``False``.
    D : bool
        BART flag ``-D``: projection angle in [0,360°), else in [0,180°)
        Default ``False``.
    O : bool
        BART flag ``-O``: correct transverse gradient error for radial
        tajectories Default ``False``.
    flag_3 : bool
        BART flag ``-3``: 3D Default ``False``.
    c : bool
        BART flag ``-c``: asymmetric trajectory [DC sampled] Default ``False``.
    E : bool
        BART flag ``-E``: multi-echo multi-spoke trajectory Default ``False``.
    x : int | None
        BART flag ``-x x``: readout samples Default ``None``.
    y : int | None
        BART flag ``-y y``: phase encoding lines Default ``None``.
    z : int | None
        BART flag ``-z z``: second phase encoding lines Default ``None``.
    d : int | None
        BART flag ``-d d``: full readout samples Default ``None``.
    e : int | None
        BART flag ``-e e``: number of echoes Default ``None``.
    a : int | None
        BART flag ``-a a``: acceleration Default ``None``.
    t : int | None
        BART flag ``-t t``: conf.turns Default ``None``.
    m : int | None
        BART flag ``-m mb``: SMS multiband factor Default ``None``.
    s : int | None
        BART flag ``-s # tiny GA``: tiny golden angle Default ``None``.
    o : float | None
        BART flag ``-o o``: oversampling factor Default ``None``.
    R : float | None
        BART flag ``-R phi``: rotate [°] Default ``None``.
    q : tuple[float, float, float] | None
        BART flag ``-q delays``: gradient delays: y, x, yx Default ``None``.
    Q : tuple[float, float, float] | None
        BART flag ``-Q delays``: (gradient delays: z, yz, xz) Default ``None``.
    C : str | None
        BART flag ``-C file``: custom_angle file [phi + i * psi] Default
        ``None``.
    V : str | None
        BART flag ``-V file``: (custom_gdelays) Default ``None``.
    double_base : bool
        BART flag ``--double-base``: define GA over 2Pi base instead of default
        Pi. Default ``False``.
    mems_legacy : bool
        BART flag ``--mems-legacy``: OLD multi-echo multi-spoke trajectory
        Default ``False``.
    z_us : tuple[int, int] | None
        BART flag ``--z-us accel``: (undersampling in z-direction.) Default
        ``None``.
    raga_inc : int | None
        BART flag ``--raga-inc d``: increment of RAGA Sampling Default
        ``None``.
    **extra_flags : Any
        Additional BART ``traj`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "traj",
        [],
        output_dims,
        l=l or None,
        g=g or None,
        r=r or None,
        G=G or None,
        H=H or None,
        A=A or None,
        D=D or None,
        O=O or None,
        flag_3=flag_3 or None,
        c=c or None,
        E=E or None,
        x=x,
        y=y,
        z=z,
        d=d,
        e=e,
        a=a,
        t=t,
        m=m,
        s=s,
        o=o,
        R=R,
        q=q,
        Q=Q,
        C=C,
        V=V,
        double_base=double_base or None,
        mems_legacy=mems_legacy or None,
        z_us=z_us,
        raga_inc=raga_inc,
        **extra_flags,
    )


@bart_op
def trajcor(
    traj: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    O: bool = False,
    q: tuple[float, float, float] | None = None,
    V: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Correct delays for a given trajectory.

    Equivalent to calling ``bart trajcor`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    traj : torch.Tensor
        Input CFL array ``traj``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    O : bool
        BART flag ``-O``: correct transverse gradient error for radial
        trajectories Default ``False``.
    q : tuple[float, float, float] | None
        BART flag ``-q delays``: gradient delays: y, x, xy Default ``None``.
    V : str | None
        BART flag ``-V file``: custom_gdelays Default ``None``.
    **extra_flags : Any
        Additional BART ``trajcor`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("trajcor", [traj], output_dims, O=O or None, q=q, V=V, **extra_flags)


@bart_op
def transpose(
    input_: torch.Tensor,
    dim1: int,
    dim2: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Transpose dimensions {dim1} and {dim2}.

    Equivalent to calling ``bart transpose`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dim1 : int
        Positional argument ``dim1``.
    dim2 : int
        Positional argument ``dim2``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``transpose`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("transpose", [input_], output_dims, dim1=dim1, dim2=dim2, **extra_flags)


@bart_op
def twixread(
    dat_file: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Read data from Siemens twix (.dat) files.

    Equivalent to calling ``bart twixread`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    dat_file : torch.Tensor
        Input CFL array ``dat file``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``twixread`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("twixread", [dat_file], output_dims, **extra_flags)


@bart_op
def unwrap(
    input_: torch.Tensor,
    dim: int,
    *,
    output_dims: list[int] | None = None,
    b: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Unwrap along selected dimensions.

    Equivalent to calling ``bart unwrap`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    dim : int
        Positional argument ``dim``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    b : float | None
        BART flag ``-b bounds``: bounds (default: PI) Default ``None``.
    **extra_flags : Any
        Additional BART ``unwrap`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("unwrap", [input_], output_dims, b=b, dim=dim, **extra_flags)


@bart_op
def upat(
    *,
    output_dims: list[int] | None = None,
    Y: int | None = None,
    Z: int | None = None,
    y: int | None = None,
    z: int | None = None,
    c: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Create a sampling pattern.

    Equivalent to calling ``bart upat`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    Y : int | None
        BART flag ``-Y Y``: size Y Default ``None``.
    Z : int | None
        BART flag ``-Z Z``: size Z Default ``None``.
    y : int | None
        BART flag ``-y uy``: undersampling y Default ``None``.
    z : int | None
        BART flag ``-z uz``: undersampling z Default ``None``.
    c : int | None
        BART flag ``-c cen``: size of k-space center Default ``None``.
    **extra_flags : Any
        Additional BART ``upat`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("upat", [], output_dims, Y=Y, Z=Z, y=y, z=z, c=c, **extra_flags)


@bart_op
def var(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Compute variance along selected dimensions specified by the {bitmask}

    Equivalent to calling ``bart var`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``var`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("var", [input_], output_dims, bitmask=bitmask, **extra_flags)


@bart_op
def vec(
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Create a vector of values.

    Equivalent to calling ``bart vec`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``vec`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("vec", [], output_dims, **extra_flags)


@bart_op
def walsh(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    r: tuple[int, int, int] | None = None,
    R: tuple[int, int, int] | None = None,
    b: tuple[int, int, int] | None = None,
    B: tuple[int, int, int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Estimate coil sensitivities using walsh method (use with ecaltwo).

    Equivalent to calling ``bart walsh`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    r : tuple[int, int, int] | None
        BART flag ``-r cal_size``: Limits the size of the calibration region.
        Default ``None``.
    R : tuple[int, int, int] | None
        BART flag ``-R``: () Default ``None``.
    b : tuple[int, int, int] | None
        BART flag ``-b block_size``: Block size. Default ``None``.
    B : tuple[int, int, int] | None
        BART flag ``-B``: () Default ``None``.
    **extra_flags : Any
        Additional BART ``walsh`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("walsh", [input_], output_dims, r=r, R=R, b=b, B=B, **extra_flags)


@bart_op
def wave(
    maps: torch.Tensor,
    wave: torch.Tensor,
    kspace: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    f: bool = False,
    H: bool = False,
    v: bool = False,
    w: bool = False,
    l: bool = False,
    r: float | None = None,
    b: int | None = None,
    i: int | None = None,
    s: float | None = None,
    c: float | None = None,
    t: float | None = None,
    e: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform a wave-caipi reconstruction.

    Equivalent to calling ``bart wave`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    maps : torch.Tensor
        Input CFL array ``maps``.
    wave : torch.Tensor
        Input CFL array ``wave``.
    kspace : torch.Tensor
        Input CFL array ``kspace``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: use GPU Default ``False``.
    f : bool
        BART flag ``-f``: Reconstruct using FISTA instead of IST. Default
        ``False``.
    H : bool
        BART flag ``-H``: Use hogwild in IST/FISTA. Default ``False``.
    v : bool
        BART flag ``-v``: Split result to real and imaginary components.
        Default ``False``.
    w : bool
        BART flag ``-w``: Use wavelet. Default ``False``.
    l : bool
        BART flag ``-l``: Use locally low rank across the real and imaginary
        components. Default ``False``.
    r : float | None
        BART flag ``-r lambda``: Soft threshold lambda for wavelet or locally
        low rank. Default ``None``.
    b : int | None
        BART flag ``-b blkdim``: Block size for locally low rank. Default
        ``None``.
    i : int | None
        BART flag ``-i mxiter``: Maximum number of iterations. Default
        ``None``.
    s : float | None
        BART flag ``-s stepsz``: Step size for iterative method. Default
        ``None``.
    c : float | None
        BART flag ``-c cntnu``: Continuation value for IST/FISTA. Default
        ``None``.
    t : float | None
        BART flag ``-t toler``: Tolerance convergence condition for iterative
        method. Default ``None``.
    e : float | None
        BART flag ``-e eigvl``: Maximum eigenvalue of normal operator, if
        known. Default ``None``.
    **extra_flags : Any
        Additional BART ``wave`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "wave",
        [maps, wave, kspace],
        output_dims,
        g=g or None,
        f=f or None,
        H=H or None,
        v=v or None,
        w=w or None,
        l=l or None,
        r=r,
        b=b,
        i=i,
        s=s,
        c=c,
        t=t,
        e=e,
        **extra_flags,
    )


@bart_op
def wavelet(
    input_: torch.Tensor,
    bitmask: int,
    *,
    output_dims: list[int] | None = None,
    a: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform wavelet transform.

    Equivalent to calling ``bart wavelet`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    bitmask : int
        Positional argument ``bitmask``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    a : bool
        BART flag ``-a``: adjoint (specify dims) Default ``False``.
    **extra_flags : Any
        Additional BART ``wavelet`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("wavelet", [input_], output_dims, a=a or None, bitmask=bitmask, **extra_flags)


@bart_op
def wavepsf(
    *,
    output_dims: list[int] | None = None,
    c: bool = False,
    x: int | None = None,
    y: int | None = None,
    r: float | None = None,
    a: int | None = None,
    t: float | None = None,
    g: float | None = None,
    s: float | None = None,
    n: int | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Generate a wave PSF in hybrid space.

    Equivalent to calling ``bart wavepsf`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    c : bool
        BART flag ``-c``: Set to use a cosine gradient wave Default ``False``.
    x : int | None
        BART flag ``-x RO_dim``: Number of readout points Default ``None``.
    y : int | None
        BART flag ``-y PE_dim``: Number of phase encode points Default
        ``None``.
    r : float | None
        BART flag ``-r PE_res``: Resolution of phase encode in cm Default
        ``None``.
    a : int | None
        BART flag ``-a ADC_T``: Readout duration in microseconds. Default
        ``None``.
    t : float | None
        BART flag ``-t ADC_dt``: ADC sampling rate in seconds Default ``None``.
    g : float | None
        BART flag ``-g gMax``: Maximum gradient amplitude in Gauss/cm Default
        ``None``.
    s : float | None
        BART flag ``-s sMax``: Maximum gradient slew rate in Gauss/cm/second
        Default ``None``.
    n : int | None
        BART flag ``-n ncyc``: Number of cycles in the gradient wave Default
        ``None``.
    **extra_flags : Any
        Additional BART ``wavepsf`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "wavepsf",
        [],
        output_dims,
        c=c or None,
        x=x,
        y=y,
        r=r,
        a=a,
        t=t,
        g=g,
        s=s,
        n=n,
        **extra_flags,
    )


@bart_op
def whiten(
    input_: torch.Tensor,
    ndata: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    n: bool = False,
    o: str | None = None,
    c: str | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Apply multi-channel noise pre-whitening on <input> using noise data <ndata>.

    Equivalent to calling ``bart whiten`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    ndata : torch.Tensor
        Input CFL array ``ndata``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    n : bool
        BART flag ``-n``: normalize variance to 1 using noise data <ndata>
        Default ``False``.
    o : str | None
        BART flag ``-o <optmat_in>``: use external whitening matrix <optmat_in>
        Default ``None``.
    c : str | None
        BART flag ``-c <covar_in>``: use external noise covariance matrix
        <covar_in> Default ``None``.
    **extra_flags : Any
        Additional BART ``whiten`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("whiten", [input_, ndata], output_dims, n=n or None, o=o, c=c, **extra_flags)


@bart_op
def window(
    input_: torch.Tensor,
    flags: int,
    *,
    output_dims: list[int] | None = None,
    H: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Apply Hamming (Hann) window to <input> along dimensions specified by flags

    Equivalent to calling ``bart window`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    flags : int
        Positional argument ``flags``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    H : bool
        BART flag ``-H``: Hann window Default ``False``.
    **extra_flags : Any
        Additional BART ``window`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("window", [input_], output_dims, H=H or None, flags=flags, **extra_flags)


@bart_op
def wshfl(
    maps: torch.Tensor,
    wave: torch.Tensor,
    phi: torch.Tensor,
    reorder: torch.Tensor,
    table: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    g: bool = False,
    K: bool = False,
    H: bool = False,
    v: bool = False,
    b: int | None = None,
    i: int | None = None,
    j: int | None = None,
    s: float | None = None,
    e: float | None = None,
    F: str | None = None,
    O: str | None = None,
    t: float | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Perform a wave-shuffling reconstruction.

    Equivalent to calling ``bart wshfl`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** GPU-capable when BART is compiled with
        ``USE_CUDA=ON`` (pass ``g=True`` to enable).

    Parameters
    ----------
    maps : torch.Tensor
        Input CFL array ``maps``.
    wave : torch.Tensor
        Input CFL array ``wave``.
    phi : torch.Tensor
        Input CFL array ``phi``.
    reorder : torch.Tensor
        Input CFL array ``reorder``.
    table : torch.Tensor
        Input CFL array ``table``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    g : bool
        BART flag ``-g``: Use GPU. Default ``False``.
    K : bool
        BART flag ``-K``: Go from data-table to shuffling basis k-space.
        Default ``False``.
    H : bool
        BART flag ``-H``: Use hogwild. Default ``False``.
    v : bool
        BART flag ``-v``: Split coefficients to real and imaginary components.
        Default ``False``.
    b : int | None
        BART flag ``-b blkdim``: Block size for locally low rank. Default
        ``None``.
    i : int | None
        BART flag ``-i mxiter``: Maximum number of iterations. Default
        ``None``.
    j : int | None
        BART flag ``-j cgiter``: Maximum number of CG iterations in ADMM.
        Default ``None``.
    s : float | None
        BART flag ``-s admrho``: ADMM Rho value. Default ``None``.
    e : float | None
        BART flag ``-e eigval``: Eigenvalue to scale step size. (Optional.)
        Default ``None``.
    F : str | None
        BART flag ``-F frwrd``: Go from shfl-coeffs to data-table. Pass in
        coeffs path. Default ``None``.
    O : str | None
        BART flag ``-O initl``: Initialize reconstruction with guess. Default
        ``None``.
    t : float | None
        BART flag ``-t toler``: Tolerance convergence condition for FISTA.
        Default ``None``.
    **extra_flags : Any
        Additional BART ``wshfl`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch(
        "wshfl",
        [maps, wave, phi, reorder, table],
        output_dims,
        g=g or None,
        K=K or None,
        H=H or None,
        v=v or None,
        b=b,
        i=i,
        j=j,
        s=s,
        e=e,
        F=F,
        O=O,
        t=t,
        **extra_flags,
    )


@bart_op
def zeros(
    dims: int,
    *,
    output_dims: list[int] | None = None,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Create a zero-filled array with {dims} dimensions of size {dim1} to {dimn}.

    Equivalent to calling ``bart zeros`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    dims : int
        Positional argument ``dims``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    **extra_flags : Any
        Additional BART ``zeros`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("zeros", [], output_dims, dims=dims, **extra_flags)


@bart_op
def zexp(
    input_: torch.Tensor,
    *,
    output_dims: list[int] | None = None,
    i: bool = False,
    **extra_flags: Any,
) -> torch.Tensor | tuple[torch.Tensor, ...]:
    """
    Point-wise complex exponential.

    Equivalent to calling ``bart zexp`` with the given arguments.
    See the BART documentation for full details.

    .. note::
        **CUDA:** CPU only.  CUDA tensors are automatically moved to
        CPU before dispatch and returned to the original device.

    Parameters
    ----------
    input_ : torch.Tensor
        Input CFL array ``input``.
    output_dims : list[int], optional
        Expected output shape; ``None`` to infer at runtime.
    i : bool
        BART flag ``-i``: imaginary Default ``False``.
    **extra_flags : Any
        Additional BART ``zexp`` flags forwarded directly.

    Returns
    -------
    torch.Tensor
        Result array in C-order ``complex64``.
    """
    return dispatch("zexp", [input_], output_dims, i=i or None, **extra_flags)


__all__ = [
    "affinereg",
    "avg",
    "bin",
    "bitmask",
    "bloch",
    "cabs",
    "calc",
    "caldir",
    "calmat",
    "carg",
    "casorati",
    "cc",
    "ccapply",
    "cdf97",
    "circshift",
    "coils",
    "compress",
    "conj",
    "conv",
    "conway",
    "copy",
    "cpyphs",
    "creal",
    "crop",
    "cunet",
    "delta",
    "denoise",
    "ecalib",
    "ecaltwo",
    "epg",
    "estdelay",
    "estdims",
    "estmotion",
    "estscaling",
    "estshift",
    "estvar",
    "extract",
    "fakeksp",
    "fft",
    "fftmod",
    "fftrot",
    "fftshift",
    "filter",
    "flatten",
    "flip",
    "fmac",
    "fovshift",
    "gmm",
    "grid",
    "grog",
    "hist",
    "homodyne",
    "ictv",
    "index",
    "interpolate",
    "invert",
    "ismrmrd",
    "itsense",
    "join",
    "looklocker",
    "lrmatrix",
    "mandelbrot",
    "measure",
    "mip",
    "mnist",
    "moba",
    "mobafit",
    "mobasig",
    "morphop",
    "multicfl",
    "ncalib",
    "nlinv",
    "nlinvnet",
    "nlmeans",
    "nnet",
    "noise",
    "normalize",
    "nrmse",
    "nufft",
    "nufftbase",
    "onehotenc",
    "ones",
    "pattern",
    "phantom",
    "phasepole",
    "pics",
    "pocsense",
    "poisson",
    "pol2mask",
    "poly",
    "psf",
    "pulse",
    "raga",
    "reconet",
    "repmat",
    "reshape",
    "resize",
    "rmfreq",
    "rof",
    "roistat",
    "rovir",
    "rss",
    "rtnlinv",
    "sake",
    "sample",
    "saxpy",
    "scale",
    "sdot",
    "seq",
    "show",
    "signal",
    "sim",
    "slice",
    "spow",
    "sqpics",
    "squeeze",
    "ssa",
    "std",
    "stl",
    "svd",
    "tee",
    "tensorflow",
    "tgv",
    "threshold",
    "toimg",
    "toraw",
    "traj",
    "trajcor",
    "transpose",
    "twixread",
    "unwrap",
    "upat",
    "var",
    "vec",
    "walsh",
    "wave",
    "wavelet",
    "wavepsf",
    "whiten",
    "window",
    "wshfl",
    "zeros",
    "zexp",
]
